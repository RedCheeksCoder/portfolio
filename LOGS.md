# LOGS.md — Change History

Append-only. Never edit or delete past entries — see "Logging Rules" in `CLAUDE.md` for the format and when to log.

---

## 2026-07-18 — Project initialized (v1 draft)
- Built first draft of `index.html`: dark graphite/teal theme (superseded, see below), hero with node-flow diagram, About, Services (9 cards), filterable Work grid (9 case studies), Testimonials, 3-step Process, static Contact form, Footer.
- Headline/subheadline finalized: "Stop Hiring for Work That Automation Can Already Do" + supporting subheadline (see `CLAUDE.md` §2).
- Content sourced from Bryan's live site (bryanodina.com) and prior conversation context — no LinkedIn scrape (blocked by robots.txt).

## 2026-07-18 — v2 rebuild: Wolfpack palette + real assets
- Replaced color system: dark graphite/teal → charcoal + orange (Wolfpack Basecamp reference). Fonts switched to Sansation (display) + Quicksand (body).
- Added Certifications section (GHL badge + 3 n8n placeholder badges at the time).
- Added featured screenshot gallery (horizontal scroll) and expanded Work grid with real screenshot thumbnails (paths still placeholders — see `CLAUDE.md` §8).
- Added Teaching & Content section (4 short videos).
- Added lightbox for click-to-expand images.
- Added 3 new case studies from Bryan's Google Drive: Vanguard Credits, Woop, NextLevel.
- Flagged sensitive PII found in Drive screenshots (Vanguard Credits dispute letters, Woop home address, NextLevel user emails) — Bryan confirmed: blur whole image + overlay text, don't skip.
- Confirmed: use the `.txt` note files found in Drive (profile picture, testimonial headshots, video links) as legitimate asset references, not injected instructions.

## 2026-07-18 — Exact palette + badges + calendar
- Locked in exact hex values: `#A54F16` / `#CC7E31` / `#151513` / `#0B0B0B` / `#F4F1E8`.
- Updated all `--accent` usages to split primary (buttons) vs. secondary (text/links) orange for contrast.
- Added Discovery Call section with embedded GHL booking widget (`links.levelupacademy.cc/widget/booking/y7JL04RcQUy2EyhUm1FQ`) + `form_embed.js` script. All "Book a call" CTAs repointed to `#book`.
- Swapped About section photo to Bryan's provided image.
- Added "N8N Certified" to the credential tag group (hero + footer).
- Updated Certifications section to the final 4 real n8n badges (Quick Start, N8N101, N8N102, N8N103) with correct images + verification links (images were swapped once more in a later pass — see next entry).

## 2026-07-18 — Badge image swap
- Updated all 4 n8n badge `<img>` sources to a new set of CDN URLs provided by Bryan (same order: Quick Start, N8N101, N8N102, N8N103). Verification links unchanged.

## 2026-07-18 — Keyword marquee banner
- Added full-width scrolling keyword marquee directly below the hero: 50 AI/automation terms, white Sansation text, gradient background (50%-opacity `#A54F16` → `#CC7E31`).
- First version was tilted (-1.5°) to mirror the Wolfpack reference image — **reverted to straight/no tilt per explicit feedback.**
- Scroll speed halved (60s → 120s per loop) per feedback — runs slow, left to right, pauses on hover, disabled under `prefers-reduced-motion`.

## 2026-07-18 — CLAUDE.md + LOGS.md created
- Added `CLAUDE.md` to the repo root summarizing the full project context (positioning, design system, section-by-section content, badge/asset inventory, known gaps, sensitive-data handling rules) for any agent (Claude Code or otherwise) picking up this codebase.
- Seeded this `LOGS.md` file with the reconstructed history above.

## 2026-07-18 — Wired real screenshots into gallery + work section, added 3 new content sections

- **Why:** Bryan uploaded ~150 screenshots to his GHL media storage and dropped the resulting CDN links into ~20 `.txt` files inside `Portfolio/`, organized by content category (Website captures, Automations, Payment Integration, Web applications, Miro Flowcharts, etc.). Asked to read those files, map the links onto the site, and log every change.
- **File relocated:** the working file had drifted to `portoflio.html` at the `Work/` root (misnamed, wrong folder). Moved it into `Portfolio/` and renamed it `index.html`, matching this doc.
- **Gallery ("Inside the Systems"):** all 10 remaining `images/...` placeholder paths replaced with real CDN links (gamified onboarding, AEMR pipeline, AEMR email analytics, AEMR payments, Vanguard Credits dashboard, AEMR courses, LUA Facebook community, LUA sub-accounts, LUA social planner, LUA calendar). 3 new items added: AI Agent build (Genesis Credit Restoration), Review Automation (Meritex Canada), Production Deployment (Vercel).
- **Work section:** all 11 remaining thumbnail placeholders filled with real CDN links (Vanguard Credits, Woop, NextLevel, Squirrel Insurance, The Bill Busters, Genesis Credit, AEMR, DeAnna Crawford, Level Up Academy, Funded Biz, Federal Barbers). 5 cards that previously had **no** thumbnail at all now do: Wisdom Church of Manila (donation page), Charity Lift, Meritex Canada, Celeste Nicolas Ministries (giving page), Space Coast Leads (SOP flowchart, since no plain website screenshot existed for this one).
- **New section — Process Design / SOP Flowcharts** (`#flowcharts`, added to nav as "Process Maps"): a 13-image grid of SOP diagrams pulled from `Miro Flowcharts.txt` (Barbershop, HVAC, Landscaping, Coaching, Credit Repair, Psych Clinic, PR & Marketing, Podcast Outreach, Contact Flow, and 4 Space Coast Leads diagrams). Placed between Work and Teaching, using the shared lightbox.
- **New content — A2P/10DLC Compliance strip:** added inside the existing Certifications section, showing 3 A2P approval screenshots (The Bill Busters, Squirrel Insurance, Genesis Credit). **Flag for Bryan:** the client-to-screenshot mapping was inferred from file order, not an explicit label — worth a quick visual check.
- **PII handling (per CLAUDE.md §9, critical, previously flagged but not yet resolved):** no image-editing tool is available in this environment, so the Vanguard Credits screenshots actually used (1 gallery item, 1 work-card thumbnail) are rendered with CSS `blur(14px)` plus a "some sensitive information has been blurred for privacy" overlay (`.gallery-blur`, `.work-thumb-wrap.blurred` classes) rather than a pre-blurred source file. Woop screenshot #3 and NextLevel screenshot #5 (the two other flagged images) were simply never linked in — safe alternates (Woop 1, NextLevel 1) were used instead. **Caveat documented in CLAUDE.md:** the CSS blur is render-time only; the raw unblurred image is still reachable by anyone who opens the CDN URL directly. If Bryan wants a stronger guarantee, the source images themselves need to be replaced with pre-blurred versions.
- **Files touched:** `Portfolio/index.html` (moved from `Work/portoflio.html` + edited), `Portfolio/CLAUDE.md` (updated site structure, certifications, known gaps, and sensitive-data sections to match), `Portfolio/LOGS.md` (this entry).

---

## 2026-07-18 — Case-study modal system, category-aware thumbnails, A2P moved into Work, Process Design restructure

- **Why:** Bryan reviewed the previous session's build and asked for six changes: (1) move A2P/10DLC out of Certifications into Work as its own filter category, (2) turn each Work-card thumbnail into a full case-study modal showing *every* image found for that client plus Description/Problem/Work-Done copy, (3) make Work-card thumbnails swap per selected filter category, (4) recategorize Wisdom Church of Manila and Celeste Nicolas Ministries as payments+automation only, (5) remove Space Coast Leads from the Web & Funnels category, (6) give Process Design flowcharts the same Description/Problem/Work-Done modal treatment, with Space Coast Leads consolidated to one card that reveals its 4 diagrams on expand.
- **A2P/10DLC:** removed the badge-row strip from Certifications (§6). Added a new `compliance` filter category to Work; Squirrel Insurance, The Bill Busters, and Genesis Credit now carry it, with their A2P screenshot as the thumbnail shown specifically under that filter.
- **Case-study modal (`#caseModal`):** new system replacing the old single-image lightbox click on Work-card thumbnails. Clicking a thumbnail or the new "View case study →" button opens a modal with Description / Problem Needed Solving / What Was Done, plus a grid of *every* image found for that client across every `.txt` source file (per Bryan's "everything found" instruction) — ranges from 1 image (Federal Barbers) to 18 (Genesis Credit). Each modal image opens full-size in the original simple lightbox on click. Data lives in a new `CASE_STUDIES` JS object; see `CLAUDE.md` §11 for the full architecture.
  - **All Description/Problem/Work-Done copy was drafted by the agent, not provided by Bryan** — he explicitly asked for a draft-then-review pass. Treat as unreviewed.
  - **All 8 Vanguard Credits images, Woop #3, and NextLevel #5 are included** (exhaustive scope) and individually flagged `blur:true`, rendered with CSS blur + "Blurred for privacy" overlay in both the modal grid and the full-size lightbox — see `CLAUDE.md` §9 for the updated PII-handling note.
- **Category-aware thumbnails:** filter clicks now swap a card's thumbnail to a category-specific image where one genuinely exists (Squirrel: apps/compliance; TBB: compliance; GCR: ai/automation/compliance; Wisdom Church: automation; Celeste Nicolas: automation; AEMR: automation/payments; Level Up Academy: automation) via a `data-thumbs` JSON attribute per card, read by the filter-click handler. Cards without a clearly distinct per-category image were deliberately left showing their default thumbnail across all filters rather than faking specificity.
- **Category changes:** Wisdom Church of Manila and Celeste Nicolas Ministries → `payments automation` (dropped `web`/`courses`); per Bryan, they share the *same* automation build, so Celeste Nicolas's modal reuses Wisdom Church's 5 automation images. Space Coast Leads → `automation` only (dropped `web`).
- **Process Design restructure:** 13 individual flowchart images collapsed into 10 cards. The 9 generic industry flowcharts (Barbershop, HVAC, Landscaping, Coaching, Credit Repair, Psych Clinic, PR & Marketing, Podcast Outreach, Contact Flow) were given invented business names per Bryan's explicit instruction ("make up client names") since no real client exists behind them — e.g. "Southside Cuts Barbershop," "Coastal Air HVAC Services." **The agent added a "Sample Build" disclosure tag on these 9 that Bryan didn't explicitly ask for**, to avoid the page reading as if these were verified real clients; easy to remove if he'd rather not have it (see `CLAUDE.md` §11). Space Coast Leads (the 10th, real) now shows 1 thumbnail and reveals all 4 of its SOP diagrams inside its modal.
- **Two image-to-client mappings were inferred, not explicit** — flagged for Bryan to confirm: the 5 "AutoQuote" screenshots assumed to belong to Squirrel Insurance, and 1 of 3 unlabeled "Social Media Automation" screenshots assumed to belong to The Bill Busters (based on a matching local filename).
- **Verification:** ran a Playwright script against the live file (`file://` load, no server needed) — confirmed zero console/page errors, correct card counts per filter (including the new `compliance` filter showing exactly Squirrel/TBB/GCR), correct thumbnail swap for GCR under `compliance`, GCR modal rendering all 18 images, Vanguard modal rendering all 8 images all blurred, nested lightbox open/close and Escape-key handling (closes both modal and lightbox), and both flowchart modal variants (real vs. sample-tagged). Screenshots taken and visually reviewed.
- **Files touched:** `Portfolio/index.html` (Work section, Process Design section, new CSS for case modal + button reset, new `CASE_STUDIES` data + modal JS, filter-click handler extended for thumbnail swap), `Portfolio/CLAUDE.md` (§4, §5, §6, §8, §9, §10 updated, new §11 added), `Portfolio/LOGS.md` (this entry).

---

## 2026-07-18 — Real Vanguard/AEMR thumbnails, AutoQuote reassigned to Meritex, DeAnna recategorized, button label change

- **Why:** Bryan reviewed the case-study modal build and had 9 corrections/additions: rename the "View case study" button, swap in a real (safe) Vanguard Credits website screenshot as its thumbnail, swap in a real AEMR website screenshot as its thumbnail, add a social planner + 3 automation screenshots for DeAnna Crawford (copy for that automation work hadn't been written yet), fix Squirrel Insurance's live URL, correct AutoQuote's ownership from Squirrel to Meritex, and drop DeAnna Crawford from both the `payments` and `courses` categories.
- **Checked with Bryan before acting:** item 2 ("remove the blur when the modal of vanguard credits is opened") was ambiguous against the CRITICAL PII rule in `CLAUDE.md` §9 — could have meant "don't blur the new safe thumbnail" or "unblur the real dispute-letter images too." Asked directly; confirmed it's only the former. **The 8 real dispute-letter images stay blurred** — this was not weakened.
- **Vanguard Credits:** work-card thumbnail replaced with a real website-capture screenshot (`6a5a7d07baf5f6da40225faf.png`), unblurred, no more `.work-thumb-wrap.blurred` wrapper on the card. Added as the first (unblurred) image in the `vanguard` modal; the other 8 dispute-letter images are unchanged and still blurred.
- **AEMR:** work-card thumbnail replaced with a real website-capture screenshot (`6a5a80b3aef6925d34362897.png`) — this link wasn't in any of the source `.txt` files, so it was requested directly from Bryan. The previous default (course-structure image) is now reachable via a new `courses`-filter thumbnail override instead of being lost.
- **DeAnna Crawford:** added a social planner screenshot and 3 automation screenshots to her modal; rewrote Description/Problem/Work-Done to reflect the automation + social-calendar work (previously the copy just said "payment system, course delivery, email marketing" with no automation content behind it). Categories changed from `web payments courses automation` to `web automation`; work-tags updated (dropped "Payments," added "Automation" and "Social planner"); added an `automation`-filter thumbnail override.
- **Squirrel Insurance → Meritex Canada (AutoQuote correction):** Bryan clarified Squirrel doesn't own the AutoQuote web app — Meritex does. Removed the 5 AutoQuote images and the `apps` category from Squirrel (now `web automation compliance`), removed the AutoQuote line from its description/workdone copy. Added the same 5 images to Meritex's modal, added `apps` + `automation` categories to Meritex (now `web apps automation`), added an `apps`-filter thumbnail override, and rewrote Meritex's description/problem/workdone to mention AutoQuote.
- **Squirrel Insurance URL:** `https://squirrelinsurancegroup.com` → `https://squirrelinsurance.com/home`.
- **Button label:** "View case study →" → "Learn more →" across all 16 Work cards.
- **Verification:** Playwright pass confirmed zero console/page errors, correct button text (16/16), Vanguard thumbnail unblurred with no wrapper, Vanguard modal shows 9 images (1 unblurred + 8 blurred), Squirrel URL correct and modal down to 2 images, Meritex modal up to 9 images, DeAnna's category correctly excludes her from the Payments and Courses filters while her modal shows all 5 images.
- **Files touched:** `Portfolio/index.html` (5 work-cards edited, `CASE_STUDIES` entries for `vanguard`/`squirrel`/`meritex`/`aemr`/`deanna` updated, global button-text replace), `Portfolio/CLAUDE.md` (§5, §8, §9, §11 updated), `Portfolio/LOGS.md` (this entry).

---

### Still outstanding (as of this entry)
- All Description/Problem/Work-Done copy across all 26 case studies (16 Work + 10 Process Design) is agent-drafted and needs Bryan's review — nothing in it is final.
## 2026-07-18 — Removed Gallery section, removed Sample Build disclosure, corrected employer name, hero copy tweaks

- **Why:** Bryan reviewed the page again and asked for four more changes: drop the "Inside the Systems" gallery section and fix the background-color rhythm around the gap it leaves, remove the "Sample Build — illustrative, not a named client" disclosure everywhere, correct the About-section employer name, and change the hero eyebrow + strip the em dashes from the hero subheadline.
- **Gallery section removed entirely** (not just hidden) — the whole `#gallery` section (11 screenshot items, including the blurred Vanguard Credits preview), its nav link ("Systems"), and all now-orphaned CSS (`.gallery-scroll`, `.gallery-item`, `.gallery-cap`, `.gallery-blur`, `.privacy-note`, `.work-thumb-wrap` — the last two were already unused after last session's Vanguard thumbnail swap) were deleted rather than left as dead code. **Background alternation fixed:** `#work` changed from `section alt` to plain `section` since it now directly follows `#services` (`section alt`) with Gallery no longer between them. This also means the blurred Vanguard Credits preview that used to sit in the Gallery is gone from the page entirely — a net reduction in PII exposure surface, not a relocation of it.
- **"Sample Build" disclosure removed:** deleted the `#caseSampleTag` modal element, its CSS class, its JS wiring (`caseSampleTag` references in `openCase()`), and the `<em>(Sample Build)</em>` suffix on all 9 invented Process Design card labels (e.g. "Southside Cuts Barbershop" no longer shows "(Sample Build)" anywhere, in the grid or the modal). The `sample:true/false` field is still present in `CASE_STUDIES` as inert, unused metadata — harmless to leave, easy to strip later if anyone wants a fully clean data object.
- **Employer name corrected:** "CTO at Power & Presence" → "CTO at The 414 Project" in the About bio (only place it appeared).
- **Hero copy:** eyebrow "Automation Systems" → "What I Can Do For Your Business"; subheadline's two em dashes removed and replaced with commas ("I architect and build automation systems, from CRM workflows to AI-powered processes, that handle the repetitive, time-consuming work, so you can grow without adding to payroll."). Both are listed in `CLAUDE.md`'s "Approved Copy" section, which was updated to match and to note these were explicit revisit requests.
- **Verification:** Playwright pass confirmed zero console/page errors after the section removal and CSS cleanup, `#gallery` and the "Systems" nav link are both gone, no "Sample Build" text remains anywhere on the page, hero eyebrow/subheadline read correctly, the bio mentions "The 414 Project," and `#work`/`#services` have the corrected alt/non-alt classes. Screenshots taken and visually reviewed (hero and services→work boundary).
- **Files touched:** `Portfolio/index.html` (Gallery section + nav link deleted, orphaned CSS removed, case-sample-tag markup/CSS/JS removed, About bio + hero eyebrow/subheadline edited), `Portfolio/CLAUDE.md` (§1, §2, §4, §9, §11 updated), `Portfolio/LOGS.md` (this entry).

---

## 2026-07-18 — Replaced GHL booking iframe with a custom Vercel-backed booking widget

- **Why:** Bryan asked what could be done about the GHL calendar iframe not matching the site's branding. Confirmed the real constraint first (cross-origin iframe, Same-Origin Policy blocks page CSS from reaching in; verified against GHL's actual support docs that only Primary/Background color pickers exist, only on their Neo widget style, no custom-CSS hook — not guessed). Offered the GHL-dashboard color fix as a zero-code option, but Bryan asked to build a fully custom calendar instead that (1) matches the brand exactly, (2) checks his real Google Calendar availability, and (3) still creates GHL contacts/appointments so his existing reminder-email automations keep firing. This is a genuine architecture shift — the site went from zero-backend static HTML to static HTML + a Vercel serverless API, since Google/GHL credentials can't live in client-side code. Used full Plan Mode for this one (Explore was skipped — no existing backend to explore — but a Plan subagent designed the file structure/API/frontend approach before writing any code), given the size and the number of real design decisions involved (backend host, sync depth, GHL integration depth, replace-vs-coexist) were confirmed with Bryan via AskUserQuestion first.
- **New backend** (`api/` + `package.json`/`vercel.json`/`.env.example`/`.gitignore`, all at the `Portfolio/` root):
  - `api/_lib/config.js` — `BOOKING_CONFIG`: working hours (Mon–Fri 9–5 default), Asia/Manila timezone, 30-min slots, 10-min buffer, 12h minimum notice, 30-day advance window. **All agent defaults, not yet confirmed by Bryan** — flagged in `CLAUDE.md` §8.
  - `api/_lib/slots.js` — pure, dependency-free slot-generation + timezone math (uses `Intl.DateTimeFormat` to compute real IANA offsets rather than hardcoding +08:00, so it'd still be correct if the timezone ever changed to a DST-observing one). Unit-tested directly with `node` — 12 assertions covering weekday detection, full-day slot counts, exact ISO-offset formatting, busy-block+buffer exclusion (verified a 10-11am busy block with a 10min buffer correctly removes the 9:30, 10:00, 10:30, and 11:00 slots specifically), minimum-notice exclusion, and booking-window bounds. All passed.
  - `api/_lib/googleCalendar.js` — service-account JWT auth (`googleapis` package), Freebusy query for availability, `events.insert` for booking (`sendUpdates:'none'` — documented limitation, see below).
  - `api/_lib/ghl.js` — plain `fetch` calls to GHL's v2 API (`services.leadconnectorhq.com`) for contact upsert + appointment creation. Wrapped so GHL failures never throw — returns `{ok:false, error}` instead, since the plan calls for GHL failure to be non-fatal to the visitor (see below).
  - `api/availability.js` (`GET ?date=YYYY-MM-DD`) and `api/book.js` (`POST`) — the two actual HTTP endpoints, thin handlers over the `_lib` modules.
  - **Race-condition handling:** `book.js` re-checks the specific slot's availability immediately before writing, returning `409 slot_taken` if it's gone. This narrows but doesn't fully close the race window (documented as an accepted, tolerable limitation for solo-consultant booking volume, not a silent gap).
  - **Partial-failure handling:** if the Google Calendar write succeeds but the GHL sync fails, the visitor still sees a successful booking (`ghlSynced:false` in the response) since the calendar hold is real either way — the failure is only logged server-side for Bryan to catch manually. This was an explicit design decision (confirmed via the approved plan), not an oversight.
- **New frontend widget** (`#bookingWidget` replacing `.booking-frame`/`<iframe>` in `#book`): three-step flow (month calendar + slot picker → contact form → confirmation), styled entirely with the site's existing tokens — reuses `.field`/`.btn-primary` verbatim, new `.day-cell`/`.slot-btn` styled off the existing `.filter-btn` pill pattern. Vanilla JS, IIFE-wrapped, appended after the work-filter code in the trailing `<script>`. Also deleted the GHL `form_embed.js` script tag — **the site now has zero external JS dependencies.**
- **Verification:**
  - `slots.js` pure-logic unit tests (12/12 passed, see above).
  - All new `api/*.js` files syntax-checked with `node --check`.
  - Full frontend flow tested with Playwright against a **local HTTP server** (not `file://` — fetch() from a file:// origin doesn't reliably hit route interception, so a throwaway `http.createServer` was used to serve `index.html` on localhost) with `/api/availability` and `/api/book` mocked: calendar renders, date selection loads slots, slot selection shows the form, successful submission shows the confirmation with correctly localized time, and a mocked `409 slot_taken` response correctly routes back to the date step with an inline error and reloads that date's slots. Existing site features (work filters, case-study modals) re-verified working alongside the new widget with zero regressions. Screenshots taken of both widget steps — visually confirmed on-brand (dark background, orange accents, correct fonts).
  - **Not tested: anything against real Google/GHL APIs** — impossible without real credentials, which the agent cannot create (requires Bryan's own account logins).
- **Known limitations documented (not silently accepted):** soft race-guard (see above), no native Google Calendar invite email to visitors since a bare service account can't send them without Workspace domain-wide delegation (GHL's reminder automation is the real notification channel), and GHL-sync failure being non-fatal to the visitor response.
- **This feature is built but NOT live.** It's blocked entirely on Bryan completing: (1) a Google Cloud service-account setup + sharing his calendar with it, (2) a GHL Private Integration token + Location ID + Calendar ID, (3) Vercel deployment + env vars. Full checklists for all three are in `CLAUDE.md` §12. Until that's done, deploying this code as-is would mean a booking widget with no working backend behind it.
- **Files touched:** new — `Portfolio/package.json`, `Portfolio/vercel.json`, `Portfolio/.env.example`, `Portfolio/.gitignore`, `Portfolio/api/availability.js`, `Portfolio/api/book.js`, `Portfolio/api/_lib/config.js`, `Portfolio/api/_lib/slots.js`, `Portfolio/api/_lib/googleCalendar.js`, `Portfolio/api/_lib/ghl.js`; edited — `Portfolio/index.html` (booking section HTML/CSS/JS, GHL embed script removed), `Portfolio/CLAUDE.md` (§1, §4, §7 rewritten, §8, §10, §11, new §12), `Portfolio/LOGS.md` (this entry).

---

## 2026-07-18 — Repo pushed to GitHub (RedCheeksCoder/portfolio)

- **Why:** Bryan wanted this folder pushed to a new GitHub repo under a different account than what was already configured locally.
- Found the folder was already a git repo (1 commit, "initial commit") with `origin` pointed at a different repo — `bryanodinaaiautomation-a11y/portfolio.git` — already pushed and clean. Flagged this to Bryan before touching anything rather than silently overwriting/assuming.
- Confirmed with Bryan: replace `origin` entirely with `RedCheeksCoder/portfolio.git` (not add as a second remote, not a one-off push leaving origin unchanged).
- Initial push failed with "Repository not found" — diagnosed as a private-repo access issue, not a typo: verified the cached git credentials (tied to `bryanodina.aiautomation@gmail.com` / `bryanodinaaiautomation-a11y`) could still successfully reach the *old* repo, confirming auth worked in general but that identity specifically lacked access to `RedCheeksCoder/portfolio`.
- Bryan chose to re-authenticate as the RedCheeksCoder account rather than add the old account as a collaborator. Cleared the cached GitHub credential (`git credential reject` for `host=github.com` — flagged to Bryan beforehand that this also clears the credential used for the old repo, since Git Credential Manager caches per-host, not per-repo) and re-attempted the push, which succeeded after a fresh sign-in.
- **Result:** `origin` now points at `https://github.com/RedCheeksCoder/portfolio.git`, `main` branch pushed and tracked, working tree clean.
- **Files touched:** none in the codebase — this was a git/remote operation only. `Portfolio/CLAUDE.md` §1 updated with a one-line note of the repo location. `Portfolio/LOGS.md` (this entry).

## 2026-07-18 — Simplified booking backend to GHL-only, removed Google Calendar entirely

- **Why:** Bryan asked why Google Calendar credentials were still needed if GHL's own Private Integration could handle booking. Good challenge to the earlier plan — investigated rather than defending the original design by default. Confirmed via GHL's official API docs that a `GET /calendars/:calendarId/free-slots` endpoint exists (used internally by GHL's own booking widgets), meaning GHL can serve as both the availability source and the booking target on its own. The original two-system design existed specifically to catch schedule conflicts from *outside* GHL (personal appointments, other client calls not booked through this GHL calendar) — asked Bryan directly whether that scenario applies to him. He confirmed **GHL's calendar is his full, comprehensive schedule** — nothing exists outside it — so the extra integration was solving a problem he doesn't have.
- **Removed:** `api/_lib/googleCalendar.js` deleted entirely. `googleapis` dependency removed from `package.json` (the backend now has zero dependencies — plain `fetch` throughout). The 3 `GOOGLE_*` env vars removed from `.env.example`; `service-account*.json` pattern removed from `.gitignore` since no Google service-account file will ever exist in this project now.
- **`api/_lib/config.js` trimmed:** `workingDays`, `workingHours`, and `bufferMinutes` removed — those are now GHL's own calendar settings (Bryan changes his available hours directly in GHL, no code deploy needed for that anymore, which is a genuine workflow improvement over the two-system version). Kept: `timezone`, `slotDurationMinutes` (must mirror GHL's own calendar duration setting), `minNoticeHours`, `maxAdvanceDays` as an extra guardrail layer on top of whatever GHL returns.
- **`api/_lib/slots.js` trimmed:** the pure slot-generation walk (working-hours loop, buffer-block exclusion) and `weekdayInZone` were deleted — that logic moved into GHL. Kept the timezone-offset math (`toOffsetISOString`, `zonedTimeToUtc`) since the API still needs to format/validate times in `Asia/Manila`, plus `isDateWithinBookingWindow` and a new small `meetsMinNotice` helper.
- **`api/_lib/ghl.js`:** added `getFreeSlots(startDate, endDate, timezone)` calling GHL's free-slots endpoint, parsed defensively (handles a few plausible response shapes) since GHL's docs site renders parameter/response examples client-side and couldn't be fully extracted via automated fetch — **flagged explicitly in code comments and `CLAUDE.md` §12 as needing verification against one real API call** before this can be trusted live. Removed the old `syncBookingToGhl()` dual-write wrapper (no longer needed with one system) — `book.js` now calls `upsertContact` + `createAppointment` directly.
- **`api/availability.js` / `api/book.js` rewritten** around `getFreeSlots()` instead of Google's freebusy query. `book.js` is meaningfully simpler now: one write instead of two, and any failure is a hard failure (no more `ghlSynced:false` partial-success case, since there's nothing to partially succeed against anymore).
- **Frontend untouched** — the `/api/availability` and `/api/book` request/response shapes stayed identical (`{date,timezone,slots:[{start,end}]}` and `{success,start,end,error,message}`), so `index.html`'s booking widget JS needed zero changes.
- **Verification:** trimmed unit test suite re-run (6/6 passed — offset formatting, booking-window bounds, min-notice check; the old buffer/slot-generation tests were deleted along with the code they tested). All `api/*.js` files re-syntax-checked. Full Playwright frontend regression re-run against a fresh local HTTP server with the API mocked — identical pass results to before the simplification, confirming the frontend truly didn't need to change.
- **Still not tested against the real GHL API** — this remains the single biggest open risk on this feature: the exact free-slots response shape is unverified, and `getFreeSlots()`'s defensive parsing needs to be checked against one real response during `vercel dev` testing before trusting it in production.
- **Files touched:** deleted — `Portfolio/api/_lib/googleCalendar.js`; edited — `Portfolio/package.json`, `Portfolio/.env.example`, `Portfolio/.gitignore`, `Portfolio/api/_lib/config.js`, `Portfolio/api/_lib/slots.js`, `Portfolio/api/_lib/ghl.js`, `Portfolio/api/availability.js`, `Portfolio/api/book.js`, `Portfolio/CLAUDE.md` (§1, §8, §12 rewritten); `Portfolio/LOGS.md` (this entry).

---

### Still outstanding (as of this entry)
- All Description/Problem/Work-Done copy across all 26 case studies (16 Work + 10 Process Design) is agent-drafted and needs Bryan's review — nothing in it is final.
- Confirm the A2P badge → client mapping is correct (inferred from file order).
- Confirm the Social Media Automation → The Bill Busters assumption (AutoQuote → Squirrel was already corrected to Meritex earlier).
- Decide whether the CSS-blur approach for the real Vanguard Credits dispute letters (and Woop/NextLevel) is sufficient long-term, or whether real pre-blurred image files should be swapped in instead.
- **Booking backend is built but not live** — now blocked only on GHL Private Integration setup + Vercel deployment (Google Calendar step no longer needed, see `CLAUDE.md` §12) — and on verifying GHL's free-slots response shape against a real API call, which is the single biggest risk to this working on the first real deploy.
- **Booking config values need confirmation** — timezone, meeting duration (especially — should match whatever the old GHL widget was actually set to), notice window, advance window, form fields (see `CLAUDE.md` §8). Working hours/buffer are now GHL calendar settings, not agent config.
- Contact form (the separate static one in `#contact`, not the new booking widget) still has no backend/action wired up (unchanged from prior entries).
- The `.txt` link files in `Portfolio/` are untouched — fine to leave as reference, or can be deleted once mappings are verified.

## 2026-07-18 — Hero visual replaced with animated node-flow diagram
- Implemented the "signature visual element" that `CLAUDE.md` §3 had documented as planned-but-never-built: the hero's profile photo (`.hero-photo`, `.hero-visual::before` decorative frame) is replaced with an inline, animated SVG flowchart — a copper-toned recreation of a support-ticket automation workflow (submit → business-hours routing → alert support/on-call → assignment check → priority review → resolve → resolved check → close), styled entirely in the site's existing brand tokens (`--accent`, `--accent-2`, `--surface`, `--bg-alt`).
- Animation is pure CSS/SVG, no JavaScript: a bright pulse (`stroke-dashoffset` on an SVG path with `pathLength="200"`) plus a small glowing spark (`offset-path`/`offset-distance`) travel the diagram's main "happy path" on a 13s infinite loop; each node's border brightens via its own `@keyframes` timed (by percentage) to when the pulse actually reaches it. Alternate/retry branches (the "No" paths, reminder loop, resolve-retry loop) render as static dim copper traces rather than animating, to keep the effect readable as one signal flowing through the system.
- Respects `prefers-reduced-motion` (added a new media-query block alongside the site's existing `.reveal` one) — disables the pulse/spark and all node glow animations, leaving a static diagram.
- `.hero-grid`'s column ratio flipped from `1.08fr .92fr` to `.92fr 1.08fr` (text column narrower, visual column wider) since the diagram needs more horizontal room than the old photo did to stay legible; `.hero-stats` (15+/24-7/8.7K counters) kept unchanged, now sitting below the diagram instead of below the photo.
- Verified end-to-end with a headless Playwright screenshot of the actual `index.html` (no dev server needed, static file) — confirmed the diagram renders, the pulse animates, node text is correct/legible (including cropping in on the "End" pill, which looked garbled at low screenshot resolution but reads correctly at higher DPI), and `console --errors` was empty.
- This was prototyped first as a standalone demo file (`Portfolio/Diagrams/hero-flow-animation.html`) per the user's request to build/preview it before wiring into the real page — that file still exists as a reference/demo copy and was not deleted.
- **Files touched:** `Portfolio/index.html` (hero CSS + markup), `Portfolio/Diagrams/hero-flow-animation.html` (standalone demo, created in an earlier session), `Portfolio/CLAUDE.md` (§3, §4 updated to describe the new hero visual), `Portfolio/LOGS.md` (this entry).
- **Still open:** the diagram's content (ticket-support workflow) is generic/illustrative, not tied to a real client system — same "no disclosure needed" treatment as the invented Process Design case studies (§11) applies conceptually, but worth Bryan's sign-off that a generic support-flow visual (rather than something automation-specific like the previously-documented Trigger→Automate→Notify concept) is the right message for the hero.

## 2026-07-18 — Got the booking backend actually live on Vercel; found and fixed deployment blockers unrelated to the code itself

- **Why:** Bryan added the GHL env vars to Vercel and asked to verify the booking backend actually works. What followed was a long chain of *infrastructure* problems — none of them bugs in the application code itself, all either Vercel account/project configuration or one wrong credential value.
- **Problem 1 — testing the wrong deployment:** the GitHub-connected project had a deploy stuck building forever with zero logs. Diagnosed as GHL Private Integration and Vercel's built-in `GET /calendars/:calendarId/free-slots` API being irrelevant to this — the actual cause was **"Deployment Blocked: commit author did not have contributing access"** — Vercel's Hobby plan won't build a commit unless the git author is recognized as having access to a *private* repo. Fixed by making `RedCheeksCoder/portfolio` public via the GitHub API (repo has no secrets committed, `.env` stays gitignored) — cheaper and simpler than reconciling git-identity-vs-Vercel-account access.
- **Problem 2 — two duplicate Vercel projects** (`portfolio` and `portfolio-ud47`) both connected to the same GitHub repo, apparently from importing twice. `portfolio` had never actually deployed and had zero env vars; deleted it, kept `portfolio-ud47` (the one Bryan had already added the correct GHL env vars to).
- **Problem 3 — stale build cache:** a "successful" deploy (`readyState: READY` in ~1s) turned out to still be running old code — Vercel had restored a build cache from before the GHL-only rewrite instead of rebuilding. Fixed by deploying with `vercel --prod --force` (skips cache) — this needs to be the default going forward for this project until proven unnecessary.
- **Problem 4 — stale alias, not stale deployment:** even after a genuinely fresh build, the public `portfolio-ud47.vercel.app` URL kept serving an old deployment because that specific alias was pinned to whichever deployment had last been *manually* assigned to it (`vercel alias set`), not automatically updated by new `vercel --prod` runs. Had to explicitly `vercel alias set <newest-deployment> portfolio-ud47.vercel.app` after every deploy in this session. **This is a recurring gotcha for future deploys** — a plain `vercel --prod` does not guarantee the project's named aliases point at the new build.
- **Problem 5 — the actual GHL bug:** once really testing the new code, `/api/availability` returned `200` with empty `slots: []`, no error. Added temporary debug logging (`console.error`, since `console.log` didn't surface in `vercel logs` — error-level only) to capture the raw GHL response and the exact request URL. Found: `GHL_CALENDAR_ID` was set to `y7JL04RcQUy2EyhUm1FQ` — the public URL slug from the *old* iframe embed, not GHL's actual internal Calendar ID. GHL returns `200 {"traceId":...}` for this (no error), which is why it looked like a code/parsing bug rather than a bad credential. **Debug logging removed after diagnosis — this was never committed to git, only briefly live via direct CLI deploys.** Bryan still needs to find the real Calendar ID in GHL (Settings → Calendars → [calendar] → its own ID field, not the booking-widget URL) and update the Vercel env var — this is the one thing still blocking real slots from showing.
- **Problem 6 — nearly triggered a Pro-plan upgrade prompt:** stacking a second CLI deploy on top of an already-stuck git-triggered one appears to have hit a concurrency-related gate that surfaced as a Vercel Pro upgrade prompt. **No payment was made.** Cleared by removing the stuck deployments (`vercel remove <deployment-url>`) rather than upgrading — confirmed with Bryan first that nothing had been paid for before touching anything further.
- **Problem 7 — "Unexpected token '<', <!DOCTYPE" crash on date-click:** Bryan reported the frontend crashing whenever a calendar date was clicked. Root cause: **Vercel's SSO/Deployment Protection** (`ssoProtection.deploymentType: "all_except_custom_domains"`) was gating every production URL Bryan was actually using (only the one specific alias I'd manually been testing against was exempt) — so `fetch('/api/availability')` was getting Vercel's HTML SSO-login redirect page back instead of JSON, and `res.json()` choked on the `<!DOCTYPE` HTML. Fixed with `vercel project protection disable portfolio-ud47 --sso`. Verified all three known production URLs (`portfolio-ud47.vercel.app`, the team-scoped domain, and the git-main branch alias) return clean `200` JSON afterward.
- **Verification:** all three production-facing URLs now return valid JSON from `/api/availability` (empty `slots: []` until the Calendar ID is fixed, but no crashes). GHL auth, the API call itself, and response parsing are all confirmed working against the real GHL API — the *only* remaining blocker for real availability data is the one wrong env var value.
- **Files touched:** `Portfolio/api/_lib/ghl.js` (temporarily, for debug logging — reverted, no net diff from the previous commit), `Portfolio/LOGS.md` (this entry). No other code changes — everything else this session was Vercel/GitHub account and project configuration, done via CLI (`vercel`, `git`, and one direct GitHub API call to flip repo visibility).
- **Still outstanding:** Bryan needs to (1) find and set the correct `GHL_CALENDAR_ID`, (2) decide whether to keep `RedCheeksCoder/portfolio` public long-term or revisit repo access another way, (3) remember `vercel --prod --force` + manual `vercel alias set` is the reliable deploy pattern for this project until/unless the git-triggered auto-deploy path is revisited.

## 2026-07-18 — Correction: "Problem 5" above was misdiagnosed — GHL_CALENDAR_ID was correct all along; booking backend is now fully working

- **Why:** Bryan pushed back on the previous entry's "Problem 5" diagnosis, sending a screenshot from GHL's own dashboard showing the calendar named "GHL Implementation - Discovery Call" with **ID: `y7JL04RcQUy2EyhUm1FQ`** — the exact value that had been flagged as "wrong" (assumed to be only the public widget URL slug, not a real Calendar ID). Correct call to push back — the earlier diagnosis was an assumption based on general GHL knowledge, not something actually confirmed for this account, and it was wrong.
- **Re-investigated properly this time:** rather than guessing again, pulled real credentials down locally via `vercel env pull --environment production` to test directly — but the environment's safety layer redacts real secret values to `[SENSITIVE]` even in a locally-sourced shell variable, so direct local `curl` testing wasn't possible (this is intentional, expected behavior, not a bug to route around). Pivoted to a **temporary diagnostic API route** (`api/debugfreeslots.js`, deployed live then deleted) that tried 5 request variants server-side (where Vercel holds the real, unredacted secrets) and returned all results in one response.
- **Findings from the real API:**
  - `startDate`/`endDate` **must** be epoch-millisecond numbers — GHL returns `422 "must be a number"` for ISO date strings. Confirms the existing code's format was already correct.
  - `locationId` must **not** be included as a query param on this endpoint — `422 "property locationId should not exist"` if sent. Good that the code never added it.
  - **The single-day test date (2026-07-20, a Monday) genuinely has no configured availability** on this calendar — that's the real reason every earlier test returned `{"traceId":...}` with no slots, not a bad Calendar ID. Widening the debug query to a 2-week range confirmed real slots exist starting **2026-07-22 (Wednesday), 9:00 PM–11:30 PM Asia/Manila**.
  - Confirmed the exact response shape: `{"YYYY-MM-DD":{"slots":["<ISO time>", ...]}}` — matches what `getFreeSlots()` was already parsing correctly.
- **Fix:** none needed in application code — deleted the temporary debug endpoint, redeployed clean, re-pointed all three production aliases. `GET /api/availability?date=2026-07-22` now returns real slot data end-to-end, confirmed live.
- **Corrected in `CLAUDE.md` §12:** removed the incorrect "wrong Calendar ID" bug note and the instruction to distrust the widget-URL-slug-as-Calendar-ID assumption; replaced with the confirmed-correct finding. §8 gap #7 marked resolved.
- **Flagged for Bryan (not a bug, just worth confirming):** the real availability window that came back is 9:00–11:30 PM, evening hours — worth a quick check in GHL that this is actually the intended schedule, since it's unusual for a "discovery call" calendar.
- **Files touched:** `Portfolio/api/debugfreeslots.js` (created, used, deleted — never left in the repo or committed), `Portfolio/CLAUDE.md` (§1, §12, §8 corrected), `Portfolio/LOGS.md` (this entry).
- **Lesson for future sessions:** don't assume a URL slug and an internal ID are different without checking — and when a live-credentials investigation is needed, test server-side via a temporary endpoint rather than trying to extract real secrets into the local shell (the safety redaction is intentional and correct; work with it, not around it).

## 2026-07-18 — 6th certification badge, badge hover glow, bigger Process Design cards

- **New certification badge:** added a 6th badge card to `#certifications` — Six Sigma Yellow Belt. Identified the image by downloading and visually inspecting both CDN URls Bryan supplied (not just trusting the filenames): the badge icon (`6a5ab2bd7fb178795975d5ce.png`) is the shield-shaped Six Sigma Yellow Belt badge, and the link target (`6a5aaf94bc82ab9d9ef51635.png`) is Bryan's actual signed certificate (John Bryan Odina, Yellow Belt, issued by Prof. Dr. Marcelo Machado Fernandes, Oct 19 2025). Follows the same pattern as the existing GHL badge — `href` points straight at an image (opens the certificate in a new tab) rather than a separate verify page, since none exists for this credential. This badge visually backs up the "Six Sigma Yellow Belt" credential tag that was already present in the hero/footer but previously had no supporting badge.
- **Badge hover effect strengthened:** `.badge-card:hover` lift increased from `translateY(-3px)` to `-5px`, and a new pulsing glow (`@keyframes badge-glow`, animated `box-shadow` in the brand orange) was added on hover — previously hover only changed the border color. Disabled under `prefers-reduced-motion` (new media-query block added, matching the site's existing convention of respecting that setting everywhere else motion is used).
- **Process Design (`#flowcharts`) cards enlarged:** `.flow-grid` gap increased from `14px` to `20px` (uniform across breakpoints), and the desktop column count dropped from `repeat(5,1fr)` to `repeat(4,1fr)` at the `min-width:980px` breakpoint — mobile (2 cols) and tablet (3 cols) breakpoints unchanged. Net effect: roughly 25% bigger cards on desktop with a consistent 20px gutter everywhere.
- **Verification:** screenshotted the real `index.html` headlessly (Playwright, no dev server needed) — confirmed the new badge renders with the correct image/link, the hover lift+glow triggers correctly (screenshotted mid-hover), the Process Design grid now shows 4 columns with visibly larger cards, and `console --errors` was empty throughout.
- **Files touched:** `Portfolio/index.html` (certifications markup + CSS, badge hover CSS, flow-grid CSS), `Portfolio/CLAUDE.md` (§6 badge table + hover note), `Portfolio/LOGS.md` (this entry).

## 2026-07-18 — Mobile hamburger nav + hero visual hidden on mobile

- **Why:** below the existing 880px nav breakpoint, `.nav-links` and `.nav-cta` were both simply `display:none` with no replacement — mobile visitors had no way to reach any section other than the hero without manually scrolling the whole page. Separately, the hero's animated node-flow diagram (added earlier this session) is detailed/text-heavy and becomes illegible at true phone widths, so Bryan asked for it (and the stat counters below it) hidden on mobile rather than shrunk further.
- **Hamburger menu added:** new `.nav-toggle` button (three-line icon that animates into an X via `nth-child` transforms) sits in `.nav`, visible only below the existing 880px breakpoint. Clicking it toggles a `.mobile-menu` dropdown (new `<nav>` sibling of `.nav` inside `<header>`) containing all 7 nav links plus a full-width "Book a call" button — same link targets as the desktop `.nav-links`. Menu fades/slides in via `opacity`/`transform`/`visibility` transition (no JS animation needed).
- **Smooth-scroll needed no new code** — `html{scroll-behavior:smooth}` was already global sitewide, so the existing `href="#section"` anchors already animate the scroll; the only new JS is the open/close toggle plus auto-closing the menu when a link is clicked or Escape is pressed (`closeMobileMenu()`, wired alongside the existing lightbox/case-modal JS at the top of the trailing `<script>` block).
- **Hero visual hidden on mobile:** `.hero-visual` (wraps both the animated `.hero-flow` diagram and `.hero-stats` counters) gets `display:none` under a new `@media(max-width:639px)` rule — the site's existing 640px breakpoint convention (already used by `.flow-grid`/`.services-grid`) was reused rather than inventing a new one. Above 640px the diagram still shows as before.
- **Verification:** headless Playwright at a 390×844 mobile viewport against the real `index.html` (no dev server needed) — confirmed `.hero-visual` computed `display:none`, hamburger click opens the menu (`aria-expanded` flips to `"true"`, icon animates to X), clicking a link both closed the menu and actually scrolled the page (`window.scrollY` moved from 0 to 3004), and `console --errors` was empty throughout.
- **Files touched:** `Portfolio/index.html` (nav CSS/HTML, hero CSS, trailing script), `Portfolio/LOGS.md` (this entry).

## 2026-07-18 — Book section switched to side-by-side layout (matches Contact)

- **Why:** Bryan wanted the booking calendar to sit to the right of the "Book your free discovery call" heading/copy, same two-column arrangement as the Contact section below it, instead of stacked with the calendar full-width underneath.
- **Change:** `#book`'s `.wrap` now reuses the existing `.contact-grid` class (already defined for Contact: `.9fr 1.1fr` two-column at ≥900px, single column below that) instead of a plain `.wrap`. No new CSS needed — same pattern, same breakpoint behavior, reused rather than duplicated. `.section-head`'s default `margin-bottom:48px` was overridden to `0` inline for this instance so the copy doesn't leave a large gap now that it's a column item instead of a full-width block sitting above the widget.
- **Verification:** Playwright screenshots at both a 1300px desktop viewport (confirmed clean side-by-side: copy left, calendar right, matches the Contact section's visual pattern) and a 420px mobile viewport (confirmed clean single-column stack, same as Contact does at that width) — no dev server needed, static file screenshot.
- **Files touched:** `Portfolio/index.html` (`#book` section markup only — one class added, one inline style), `Portfolio/LOGS.md` (this entry).

## 2026-07-18 — Extended the badge hover glow to Services, Work, and Process Design cards

- **Why:** Bryan liked the pulsing glow hover effect added to certification badges and asked for the same treatment on `.service-card`, `.work-card`, and `.flow-card`.
- **Change:** renamed the shared keyframes from `badge-glow` to the more generic `card-glow` (same animation, just no longer badge-specific) and added `animation:card-glow 1.6s ease-in-out infinite;` to all three cards' existing `:hover` rules, alongside their existing `border-color`/`translateY(-3px)` hover behavior (left unchanged — only the glow was added, lift amount stays card-type-specific: badges lift 5px, these three keep their existing 3px). The `prefers-reduced-motion` override that disables the animation was extended to cover all four selectors (`.badge-card`, `.service-card`, `.work-card`, `.flow-card`).
- **Verification:** headless Playwright hover screenshots of one card in each of the three sections (Services, Work, Process Design) against the real `index.html` — glow visible in all three, no console errors.
- **Files touched:** `Portfolio/index.html` (badge/service/work/flow hover CSS + keyframes rename), `Portfolio/LOGS.md` (this entry).

## 2026-07-18 — Custom domain repointed to the current production deployment (ops fix, no code change)

- **Why:** Bryan connected his custom domain `bryanodina.com` (added to the Vercel account ~10 minutes prior) but it was serving stale content. This is the exact gotcha already documented in this file's Deployment/Ops notes (#3): `vercel --prod` never auto-updates named aliases — a domain (or any alias) stays pinned to whichever deployment it was last explicitly pointed at via `vercel alias set`, even after newer deployments go live.
- **Investigated via Vercel CLI** (`vercel whoami` confirmed already logged in as `redcheekscoder`, matching the `redcheekscoders-projects` scope — the Vercel MCP connector available in this session turned out to be authenticated under a different/unrelated account that couldn't see this project at all, `get_project` 404'd on both the project ID and slug, so the CLI was used directly instead): `vercel alias ls` showed `bryanodina.com` and `www.bryanodina.com` both pointed at `portfolio-ud47-qm2s9hyrv-...vercel.app`, the **oldest** of 15 "Ready" production deployments from the last 2 hours. The newest (`portfolio-ud47-ifqhfms69-...`, 22 minutes old at the time) was already what the default `portfolio-ud47.vercel.app` alias pointed to.
- **Fix:** `vercel alias set portfolio-ud47-ifqhfms69-redcheekscoders-projects.vercel.app bryanodina.com` and the same for `www.bryanodina.com` — both succeeded immediately.
- **Verified:** fetched `https://bryanodina.com` live (follows a 308 redirect to `https://www.bryanodina.com/`, by existing domain config) and confirmed the response body now contains markers from this session's latest shipped work (`hero-flow`, `nav-toggle`, "Six Sigma Yellow Belt") that weren't in the old pinned deployment.
- **Not addressed / worth Bryan knowing:** this only repoints the domain to whatever the *last manual `vercel --prod` deploy* happened to contain. There are still uncommitted local changes (the card-glow work from the previous entry) that were never pushed to GitHub or deployed — the live domain does not yet include those. Also, per Deployment/Ops note #3, this same "alias goes stale" problem will recur on every future deploy unless the alias-repointing step becomes part of the regular deploy routine (or git-triggered auto-deploy — currently unreliable, see note #1 — gets fixed so Vercel's own domain-tracks-production behavior can take over instead of manual aliasing).
- **Files touched:** none in the repo — this was a Vercel account/infra change only (domain alias reassignment), not a code change. `Portfolio/LOGS.md` (this entry) for the record per the logging rule.

## 2026-07-18 — Extended the hover glow to the remaining card types (video, testimonial, process-step)

- **Why:** Bryan asked for the glow effect (already on badges, services, work, and Process Design cards) to be on "all the cards." Audited every `-card`-style element in `index.html`: `.video-card` (Teaching & Content) and `.test-card` (Testimonials) had no hover state at all yet; `.process-step` (the 3-step Process section) is styled identically to the other cards (same `surface`/`border`/`radius` pattern) but also had no hover. `.form-card` (the static Contact form panel) was deliberately left alone — it's a singleton form container, not a repeated card in a grid, so a hover-lift/glow wouldn't make sense there (no click-through purpose, and it wraps interactive form fields).
- **Change:** added `transition:border-color .2s, transform .2s;` to each of the three base rules (matching the existing card pattern) and a `:hover` rule with `border-color:var(--accent); transform:translateY(-3px); animation:card-glow 1.6s ease-in-out infinite;` — identical treatment to `.service-card`/`.work-card`/`.flow-card`. Extended the shared `prefers-reduced-motion` override to disable the animation on these three too.
- **Verification:** headless Playwright hover screenshots of one card in each of the three sections against the real `index.html` — glow visible in all three, no console errors.
- **Files touched:** `Portfolio/index.html` (video-card/test-card/process-step CSS), `Portfolio/LOGS.md` (this entry).

## 2026-07-18 — Shipped everything live: committed, pushed, deployed, and re-aliased

- **Why:** Bryan asked to confirm whether the recent card-glow work had actually gone live — it hadn't. Everything since the "Mobile hamburger nav" commit (the 6th cert badge + hover glow + Process Design grid sizing, and the two rounds of card-glow extension) was still sitting as uncommitted local changes; nothing had been pushed to GitHub or redeployed, so the domain fix from two entries ago just repointed to the last *old* deploy.
- **Committed & pushed:** `git commit` (`5cbb5e7`, "Extend hover glow effect to all card types") and `git push origin main` — both `index.html` and `LOGS.md` diffs (36 insertions / 9 deletions) went up cleanly, no conflicts.
- **Deployed:** `vercel --prod --yes --force --scope redcheekscoders-projects` per the documented process (Deployment/Ops notes #2 — `--force` to skip any stale build cache). New production deployment: `portfolio-ud47-kmo3p4rn2-redcheekscoders-projects.vercel.app`, build completed in ~1s, `readyState: READY`.
- **Re-aliased everything** (per Deployment/Ops notes #3 — `vercel --prod` never moves named aliases on its own): repointed `bryanodina.com`, `www.bryanodina.com`, `portfolio-ud47.vercel.app`, `portfolio-ud47-redcheekscoders-projects.vercel.app`, and `portfolio-ud47-git-main-redcheekscoders-projects.vercel.app` to the new deployment. (`portfolio-ud47-redcheekscoder-redcheekscoders-projects.vercel.app` was auto-aliased by the deploy itself.)
- **Verified:** fetched `https://bryanodina.com` live — 200 OK, response body contains `card-glow` (this session's newest class name), confirming the custom domain now serves the actual latest code, not a stale snapshot.
- **Files touched:** `Portfolio/index.html`, `Portfolio/LOGS.md` (committed as `5cbb5e7`); this entry documents the deploy/alias step, which touches no repo files itself.

## 2026-07-18 — About section restructure, orange CTA, one-line hero credential tags

- **About section:** the "My focus is simple..." sentence moved up (now appears right after the opening bio paragraph), and the old "Today I work as CTO at The 414 Project... Level Up Academy..." sentence was rewritten as a single pipe (`|`)-separated line with exactly 3 segments (CTO at The 414 Project / GHL expert at Wisdom Church of Manila / runs Level Up Academy), placed below the "My focus is simple" line per Bryan's requested order. Same bolded proper nouns kept from the original copy.
- **"Book a call with me" CTA:** swapped from `btn-ghost` (transparent, bordered) to `btn-primary` (solid `var(--accent)` orange background) — same button style already used everywhere else on the site for primary CTAs, so no new CSS needed.
- **Hero credential tags** (`Licensed Electronics Engineer` / `GHL Certified Admin` / `Six Sigma Yellow Belt` / `N8N Certified`): were wrapping to 2 lines within the hero's text column at normal desktop widths. Shrank `.cred-tag` from 11px/`.08em` letter-spacing/7px×12px padding down to 8px/`.02em`/4px×6px, and `.creds` gap from 10px to 4px; added `white-space:nowrap` per tag. Confirmed via Playwright bounding-box check that all 4 tags now share the same `top` offset (one line) at a 1400px viewport, where the hero text column is capped at the site's own `--maxw` (1180px) content width. **Caveat, not fixed:** at the narrower end of the 2-column breakpoint (~920–1100px) and on true mobile phones, the 4 tags still wrap — `flex-wrap` was deliberately left in place as a graceful fallback rather than forcing single-line via horizontal scroll or illegibly tiny text; fitting one line at every possible viewport width wasn't what was asked.
- **Verification:** headless Playwright screenshots of the About section and hero (1400px viewport) against the real `index.html`, plus a bounding-box measurement pass on `.cred-tag` — no console errors.
- **Files touched:** `Portfolio/index.html` (About section markup, `.creds`/`.cred-tag` CSS), `Portfolio/LOGS.md` (this entry). **Not yet committed/pushed/deployed** — still local changes as of this entry.

## 2026-07-18 — Added a standing "Deploy Rules" section to CLAUDE.md, then shipped the previous entry's pending changes

- **Why:** Bryan noticed the About/CTA/hero-tags changes from the previous entry were still sitting local, uncommitted, and undeployed after being reported as "done." He asked for a standing rule in `CLAUDE.md` so this doesn't keep happening — every code change should reach production in the same session, not just get saved to the file.
- **Added a new "Deploy Rules" section to `CLAUDE.md`** (right after Logging Rules, same "always do this, don't wait to be asked" framing): commit + push, `vercel --prod --yes --force`, re-point every alias (the custom domain included — this was the exact thing that went stale twice before), then verify live with a `curl` + grep for something unique to the change. Only skippable if the user explicitly says to hold off.
- **Immediately applied it**, retroactively, to the previous entry's still-pending work: committed (`82ef51c`, includes this `CLAUDE.md` change itself plus the About/CTA/hero-tag edits) and pushed to `main`, deployed to Vercel production (`portfolio-ud47-f0zxfq0zf-...`), re-aliased `bryanodina.com`/`www.bryanodina.com`/`portfolio-ud47.vercel.app`/and the two other named aliases to it, and verified live via `curl` — confirmed the pipe-separated About line and the orange CTA button are both present in the actual `bryanodina.com` response.
- **Files touched:** `Portfolio/CLAUDE.md` (new Deploy Rules section), `Portfolio/index.html`/`Portfolio/LOGS.md` (previous entry's changes, committed together in `82ef51c`). This entry itself will go out in the next commit.

## 2026-07-19 — Seven layout/sizing tweaks: contact/booking at 70%, Process Design 5×2, filter font +20%, Services as a right-to-left marquee, About copy trimmed, About photo at 70%

- **Contact form & booking calendar shrunk to 70%:** both `.form-card` and `.booking-widget` now use the CSS `zoom` property (`zoom:0.7`) rather than manually rescaling every internal font-size/padding rule — `zoom` scales an element's own layout box *and* everything inside it together (unlike `transform:scale`, which only repaints visually without affecting the box's contribution to surrounding layout). One real gotcha hit and fixed: `.form-card` sits in a CSS Grid cell with default `stretch` alignment and had no `max-width` of its own, so it stretched to the full grid track *before* zoom got a chance to act — zoom only visibly shrinks a dimension that's already an explicit/constrained value, not one resolved via `auto`/stretch. Fix: gave `.form-card` an explicit `max-width:600px` (matching `.booking-widget`'s existing `max-width:640px` pattern) so zoom has a concrete box to scale down. Verified with precise `getBoundingClientRect()` measurements (not just eyeballing a screenshot, which is too imprecise to judge a 30% size delta): `.booking-widget` → 448px (640×0.7), `.form-card` → 420px (600×0.7), and internal elements (day-cell, label, input) all measured at ~70% of their pre-zoom pixel size too.
- **Process Design grid → 5 columns × 2 rows:** `.flow-grid`'s desktop breakpoint changed from `repeat(4,1fr)` to `repeat(5,1fr)` — exact fit since there are 10 cards total.
- **Work section filter-pill font +20%:** `.filter-btn` font-size `12px → 14.4px` (interpreted "the categories" as the Work section's filter buttons — All/Web & Funnels/Automation/etc. — since that's the only place in this codebase literally called "categories," per `CLAUDE.md` §5's own terminology; flagged for Bryan to correct if a different element was meant).
- **Services section ("What I build for clients") converted from a static grid into an infinite right-to-left marquee**, matching the existing keyword-marquee pattern (duplicated content + `translateX` keyframe loop, pauses on hover, respects `prefers-reduced-motion`) rather than the left-to-right direction used by that banner. Two parallel DOM variants toggle by breakpoint (pure CSS `display`, no JS): `.services-desktop-marquee` (all 9 cards in one belt) shown ≥640px, `.services-mobile-marquee` (split into two independent belts — 5 cards + 4 cards, both moving right to left) shown below it. Card text also shrunk to ~70% (icon 17px→12px, h3 16px→11px, p 14px→10px) per Bryan's request, and each card given a fixed `260px` width so it behaves correctly as a flex/marquee item instead of a grid cell. Old `.services-grid` breakpoint CSS was removed as dead code.
- **About section copy:** the CTO/GHL/Level Up Academy line shortened to Bryan's exact requested wording — `**CTO at the 414 Project** | **GHL Expert at Wisdom Church** | Run **Level Up Academy**` (drops "a digital agency," "of Manila," and the Level Up Academy description entirely).
- **Removed "Licensed" from "Licensed Electronics Engineer"** in both places it appeared (hero credential tag, About's opening paragraph) — changed "a Licensed Electronics Engineer" to "an Electronics Engineer" in the paragraph (article agreement, since dropping "Licensed" changes it to a vowel-sound start). The footer's credential line already said "Electronics Engineer" with no "Licensed," so nothing needed there.
- **About photo shrunk to 70%:** `.about-photo` gained `max-width:70%` (relative to its own grid column, mirroring the same percentage-of-container technique already used elsewhere in this file rather than a fixed px value, since the About grid column itself is responsive).
- **Verification:** headless Playwright across a 1400px desktop viewport (About, Services motion at two timestamps, Work filters, Process Design grid, booking widget, contact form) and a 390px mobile viewport (hero tags, 2-row Services marquee) against the real `index.html` — no console errors in either pass.
- **Files touched:** `Portfolio/index.html` only (CSS + markup across About, Services, Work filters, Process Design, Booking, Contact), `Portfolio/LOGS.md` (this entry). **Not yet committed/pushed/deployed** — per the new Deploy Rules this should ship before the session is considered done.

## 2026-07-19 — Services marquee full-bleed, Process Design cards ~1.5x bigger, About photo aligned right

- **Services marquee now spans 100% of the viewport width:** both `.services-desktop-marquee` and `.services-mobile-marquee` were moved out of the section's `.wrap` (which caps at the site's `--maxw`/1180px and centers with padding) to be direct children of `<section id="services">` instead — only the heading/intro text stays inside `.wrap`. Verified the marquee's rendered width equals `window.innerWidth` exactly at a 1600px viewport.
- **Process Design cards enlarged ~1.5x:** gave the section's `.wrap` a scoped `.flow-wrap` modifier (`max-width:1700px`, up from the site-wide 1180px) so the same 5-column grid gets meaningfully more room per card; grid `gap` bumped 20px→30px and the caption `span` font-size 11.5px→17px / padding 10px 12px→15px 18px to stay proportional at the new size.
  - **Real bug found and fixed along the way:** `.flow-card` is a `<figure>` element, and figures carry a browser-default `margin: 1em 40px` that nothing in this codebase had ever reset — so every flow-card was silently 80px narrower than its actual grid column (measured 206px against a 286px column). This was invisible before because the narrower old layout didn't make the gap obviously wrong, but it meant the "5×2 grid" from the previous session was never quite exact and the "bigger" request wouldn't have visibly worked without also fixing it. Added `margin:0` to `.flow-card`. Confirmed via `getBoundingClientRect()` that card width now exactly equals the grid's own template-column width.
- **About photo aligned right:** added `margin-left:auto;display:block;` to `.about-photo` — since the previous session already capped it at `max-width:70%`, the smaller image was sitting flush-left in its grid column by default; this pushes it flush-right instead (verified the photo's right edge exactly matches its column's right edge).
- **Verification:** headless Playwright at 1600px viewport — measured marquee width vs. `window.innerWidth`, flow-card width vs. grid template-column width, and about-photo's right edge vs. its column's right edge; all matched expectations. No console errors.
- **Files touched:** `Portfolio/index.html` only, `Portfolio/LOGS.md` (this entry). **Not yet committed/pushed/deployed.**

## 2026-07-19 — Hero headline recolor: "Stop Hiring" bold orange, "Automation" white

- Wrapped "Stop Hiring" in `<strong>` and added `.hero h1 strong{color:var(--accent-2);font-weight:800;}` — bold orange, matching the same secondary-orange token already used for headline emphasis.
- Swapped `.hero h1 em`'s color from `var(--accent-2)` (orange) to `var(--text)` (white) — "Automation" now reads white like the rest of the headline instead of standing out in orange.
- **Verification:** headless Playwright screenshot of the hero headline against the real `index.html` — confirmed "Stop Hiring" renders bold/orange and "Automation" renders white. No console errors.
- **Files touched:** `Portfolio/index.html` only, `Portfolio/LOGS.md` (this entry). **Not yet committed/pushed/deployed.**

## 2026-07-19 — Work grid to 4 columns (mobile-first), new "Social Media Automation" filter category

- **Work grid now reaches 4 columns:** added a `min-width:1280px` breakpoint on top of the existing progression, and bumped the 2-column breakpoint from `720px` to `640px` to match the site's own established tablet breakpoint used everywhere else (services/flow/video/badges grids). Final mobile-first progression: 1 col (default) → 2 (640px) → 3 (1040px) → 4 (1280px). Verified via computed `gridTemplateColumns` at 390/700/1100/1400px viewports — 1/2/3/4 columns respectively.
- **New filter category "Social Media Automation"** (`data-filter="socialmedia"`) added to the Work section's filter row, tagged onto exactly the 4 requested cards: Squirrel Insurance, The Bill Busters, Genesis Credit, and Level Up Academy (appended to each card's existing `data-category` list, e.g. `squirrel`'s went from `"web automation compliance"` to `"web automation compliance socialmedia"`). Confirmed via the existing filter JS's thumbnail-swap fallback (`alt[filter] || defaultSrc`) that cards without a `socialmedia`-specific `data-thumbs` override just show their default thumbnail rather than breaking — no JS changes needed.
- **Verification:** headless Playwright — clicked the new filter button and confirmed exactly `[squirrel, tbb, gcr, lua]` show as non-hidden, screenshotted the result, no console errors.
- **Files touched:** `Portfolio/index.html` (work-grid CSS, new filter button, 4 cards' `data-category`), `Portfolio/CLAUDE.md` (§5 filter-category list), `Portfolio/LOGS.md` (this entry). **Not yet committed/pushed/deployed.**

## 2026-07-19 — Mobile fixes: About photo centered, video black-thumbnail bug fixed, video grid 2×2

- **About photo centered on mobile:** the previous session's `margin-left:auto` (right-aligns the 70%-width photo on desktop) was also applying below the `about-grid` single-column breakpoint, pushing the photo to the right on mobile too instead of centering it. Added `@media(max-width:879.98px){.about-photo{margin-left:auto;margin-right:auto;}}` to center it specifically on mobile/tablet while leaving the desktop right-alignment untouched. Verified via measured left/right gap (equal on a 390px mobile viewport; desktop still right-flush).
- **Fixed black video thumbnails on mobile — root cause found, not just patched:** the 4 Teaching & Content `<video>` elements had no `poster` attribute, relying on `preload="metadata"` to show a first-frame preview. Desktop Chromium renders that automatically; mobile browsers (iOS Safari, some Android Chrome) commonly refuse to paint a metadata-only frame and just show black until playback starts — a real, known mobile-vs-desktop `<video>` behavior gap, not a bug in this codebase's CSS. Since this site has no local `images/` folder and no upload access to Bryan's CDN, generated real poster frames instead of guessing: used a headless Chromium (Playwright) to load each video directly, seek to ~1s, and screenshot the video element to capture an actual representative frame, then embedded each as an inline `poster="data:image/jpeg;base64,..."` data URI directly on its `<video>` tag (no new external asset/hosting needed, keeps the "no build step, everything in one file" philosophy this project already follows). Added ~130KB total to `index.html` (4 JPEGs, base64-encoded) — an acceptable tradeoff for a single portfolio page with no build pipeline to optimize it further.
- **Video grid set to 2×2 on mobile:** `.video-grid`'s default (mobile) `grid-template-columns` changed from `1fr` to `repeat(2,1fr)`; the now-redundant `640px` breakpoint (which used to be the only way to get 2 columns) was removed since mobile starts at 2 columns already. The `1000px→repeat(4,1fr)` desktop breakpoint is unchanged. Verified 2 columns at a 390px viewport and 4 columns still intact at 1400px (no desktop regression).
- **Verification:** headless Playwright at both a 390px mobile and 1400px desktop viewport against the real `index.html` — photo centering (mobile) / right-alignment (desktop) both confirmed via measured gaps, video grid column counts confirmed at both sizes, all 4 video posters render actual frames instead of black boxes, no console errors either pass.
- **Files touched:** `Portfolio/index.html` only (About/video CSS + 4 video `poster` attributes), `Portfolio/LOGS.md` (this entry). **Not yet committed/pushed/deployed.**

## 2026-07-19 — Minimum-rate notice added to the booking form

- **Why:** Bryan wants to pre-qualify leads before they fill out the contact form — a short, visible note stating his minimum rate so low-ball inquiries self-select out before booking a call, rather than after.
- Added a `.booking-notice` box in `#bookingStepForm` (the step shown right after a date/time is picked, above the name/email/phone/notes fields): *"Quick note so we don't waste each other's time: my rate starts at **$12/hr** (**$15/hr** if AI agents are involved). If that's outside your budget, this likely isn't the right fit."* Styled as a small accent-bordered callout (left border in `--accent-2`, soft background, `--text-muted` body copy with the two dollar figures bolded in `--text` so they're the first thing skimmed) — noticeable without reading as an error or a wall of text.
- **Verification:** this step is only reachable through the live booking flow (`/api/availability` needs the deployed backend, which a local `file://` open can't call), so verified by programmatically revealing `#bookingStepForm` in a headless Playwright pass against the real `index.html` and screenshotting it — confirmed the notice renders correctly above the form fields, no console errors.
- **Files touched:** `Portfolio/index.html` (booking form markup + `.booking-notice` CSS), `Portfolio/LOGS.md` (this entry). **Not yet committed/pushed/deployed.**

## 2026-07-19 — Diagnosed and resolved: booking form "Could not complete the booking" error

- **Why:** Bryan reported every booking attempt on the live site failed with the generic "Could not complete the booking. Please try again." message (the 500 fallback in `api/book.js:69`), which by design doesn't surface the real cause to visitors.
- **Diagnosis (from live production evidence, not guesswork):** pulled runtime logs directly from the deployed function (`vercel logs bryanodina.com --scope redcheekscoders-projects`, since the Vercel MCP connector available in this session is authenticated to a different account and returns 403 on this project — same limitation hit in an earlier session). Found the exact stack trace on every failed attempt: `GHL upsertContact failed: 401 {"statusCode":401,"message":"The token is not authorized for this scope."}` at `api/_lib/ghl.js:77`. This confirmed the `GHL_PRIVATE_INTEGRATION_TOKEN` was missing the Contacts scope — `getFreeSlots` (calendar/free-slots scope) already worked fine, which is why the site could show open time slots even while every actual booking failed at the very next step. `createAppointment` (calendar/appointments-write scope) had never even been reached by any attempt up to this point.
- **Fix (mostly on Bryan's end, as expected — this is a GHL account permission, not application code):** Bryan added the Contacts scope (and Calendars) to the GHL Private Integration token himself and ran `vercel --prod` to redeploy with the corrected token. **Caught a second issue this surfaced:** the new deployment (`portfolio-ud47-9t5pshbaj-...`) existed, but `bryanodina.com`/`www.bryanodina.com` were still aliased to the *previous* deployment (22h old, with the old broken token) — the same "aliases don't auto-update" gotcha from `CLAUDE.md` §12 / the standing Deploy Rules, just triggered by Bryan's own manual redeploy this time instead of one of mine. Re-pointed all named aliases (including the custom domain) to the new deployment.
- **End-to-end verification:** with explicit approval, submitted one real test booking directly via `POST /api/book` (name "TEST BOOKING - please delete", a fake email) against a real open slot from `/api/availability`. Got back `{"success":true,"appointmentId":"TNUqBBafiNo8DVqjx78i",...}` — confirmed both `upsertContact` and `createAppointment` now work end-to-end for the first time. Re-checked `vercel logs` immediately after: the request logged at `info` level, not `error`. **Bryan should delete this test contact/appointment from GHL** (2026-07-23, 9:00 PM Asia/Manila) since it's not a real lead.
- **Files touched:** `Portfolio/CLAUDE.md` (§12 known-limitations entry marking this resolved), `Portfolio/LOGS.md` (this entry). No application code changed — the bug was entirely a GHL credential-scope + Vercel-alias issue, not a code defect.

## 2026-07-19 — AI Receptionist (GHL chat widget) — planning only, no code yet
- Bryan wants an AI receptionist (chat widget, floating on top of the site) using GHL's real Conversation AI / AI Employee — a genuine working integration, not a mockup.
- **Researched before building anything, since this is the same shape of problem as the old booking-calendar iframe:** GHL's Chat Widget is only customizable through its own dashboard settings (theme color, icon, avatar, position, welcome message) — no custom-CSS override, since it's an injected script/iframe (cross-origin, same constraint documented in `CLAUDE.md` §7). The Conversation AI Public API manages agents/actions and audits past conversations; it does not expose a confirmed "send visitor message → get AI reply" endpoint, so a fully custom-built chat UI wired directly to the GHL AI Employee is not a supported path per GHL's own docs.
- **Decision (Bryan's, after seeing the tradeoff):** use GHL's real embeddable Chat Widget + Conversation AI as-is, themed as closely as possible via GHL's own settings, rather than building a from-scratch custom UI backed by a different AI (e.g. Claude API) or attempting an unconfirmed custom-UI-via-Conversations-API workaround.
- **Status: blocked on Bryan.** He has not yet set up/trained the Conversation AI bot in GHL (paid add-on, uses AI credits) and does not yet have the widget embed script (Settings → Chat Widget → Get Code). Nothing added to `index.html` yet — there's nothing functional to embed until he has that script.
- **Recommended GHL widget settings to match site branding**, given to Bryan for when he configures it: primary/theme color `#A54F16` (or `#CC7E31` if only one accent slot is exposed — better contrast on dark), background `#151513`/`#0B0B0B` if exposed, position bottom-right (nothing else on the page currently occupies a fixed/floating position — confirmed by grepping `index.html` for `position:fixed`, only the case-study modal and lightbox use it, both non-floating overlays), custom icon (not GHL default), a welcome message in Bryan's voice, and "load on interaction" enabled to protect page-load performance.
- **Next step:** once Bryan pastes the embed script, add it to `index.html` immediately before `</body>` (alongside the existing inline script block), verify no conflicts with the case-modal/lightbox z-index stack (98/100) or the booking widget, then ship via the normal commit → push → `vercel --prod --force` → re-alias flow (see Deploy Rules).
- **Files touched:** `Portfolio/LOGS.md` (this entry), `Portfolio/CLAUDE.md` (new §13 added — see there for the standing reference).

## 2026-07-19 — Logo click-to-top

- Changed the header logo (`BRYAN ODINA`) from a `<div class="logo">` to `<a class="logo" href="#">` — clicking it now scrolls smoothly to the top of the page (site-wide `html{scroll-behavior:smooth}` already handles the animation, no JS needed). No CSS changes needed since `.logo`'s existing font styles apply the same to an anchor, and the sitewide `a{color:inherit;text-decoration:none;}` reset already matches its look.
- **Verification:** headless Playwright — scrolled down, clicked the logo, confirmed `window.scrollY` settles at `0`. No console errors.
- **Files touched:** `Portfolio/index.html` only, `Portfolio/LOGS.md` (this entry). **Not yet committed/pushed/deployed.**

## 2026-07-19 — Favicon added

- **Why:** Bryan asked for a favicon: max-size orange square, 2px corner radius on all sides, bold white "B" centered inside.
- **Design:** `favicon.svg` — a 32×32 viewBox `<rect>` (`rx=2`, literal 2px radius at native size, per spec) filled with the site's primary brand orange `#A54F16`, with a bold (`font-weight:700`) white-ish (`#F4F1E8`, the site's standard "white" text token, not pure `#FFFFFF`) "B" centered via `text-anchor="middle"` + `dominant-baseline="central"`. Used a generic `Arial, Helvetica, sans-serif` stack rather than the site's Sansation webfont — favicon rendering contexts (browser tabs, bookmarks) don't reliably load external webfonts, so a safe system-font fallback was used intentionally, not an oversight.
- **Exports:** rendered `favicon.svg` to PNG at the sizes actually referenced (`favicon-16x16.png`, `favicon-32x32.png`, `apple-touch-icon.png` at 180×180 for iOS) via headless Playwright screenshots of the SVG at each target size — no external image tool needed. Also generated 192×192 and 512×512 versions during exploration but deleted them since nothing on the site references them (no web manifest exists) — avoided leaving unused files in the repo.
- **Wired into `<head>`:** `<link rel="icon" href="favicon.svg" type="image/svg+xml">` (modern browsers, scalable) plus `32x32`/`16x16` PNG fallbacks and an `apple-touch-icon` link, inserted right after the meta description tag.
- **Verification:** confirmed all four linked files resolve with `200` via a local static server, and confirmed via Playwright that all four `<link>` tags in the rendered page resolve to the correct files with correct `sizes` attributes. Visually previewed the SVG at 256/64/32/16px — legible and correctly proportioned at all sizes, including native 16×16.
- **Files touched:** new — `Portfolio/favicon.svg`, `Portfolio/favicon-16x16.png`, `Portfolio/favicon-32x32.png`, `Portfolio/apple-touch-icon.png`; edited — `Portfolio/index.html` (`<head>` icon links), `Portfolio/LOGS.md` (this entry).

## 2026-07-19 — Custom domain was serving stale content — process gap, not a Vercel bug

- **Why:** Bryan reported `bryanodina.com` wasn't showing the latest deployment. Investigated: the custom domain (`bryanodina.com` + `www.bryanodina.com`, connected to this project by an earlier session, per `CLAUDE.md`'s existing "Deploy Rules" section) was still aliased to a deployment from ~1 day ago — none of this session's several redeploys (favicon, logo click-to-top, book layout, mobile nav) had re-pointed it.
- **Root cause: process gap, not a tooling bug.** `CLAUDE.md` already had a "Deploy Rules" section (added by a prior session, after the domain was connected) explicitly listing all 5 aliases to re-point after every deploy, including `bryanodina.com`/`www.bryanodina.com`. This session's earlier redeploys only re-pointed the 3 `*.vercel.app` aliases established earlier in *this* session's own context, without re-reading `CLAUDE.md`'s Deploy Rules checklist first — so the custom domain silently fell behind for several deploys. Confirmed via `vercel domains inspect bryanodina.com` and `vercel alias ls | grep bryanodina` that it was pinned to an old deployment hash while the `*.vercel.app` aliases had already moved on.
- **Fix:** `vercel alias set <newest-deployment> bryanodina.com` and same for `www.bryanodina.com`. Verified via `curl -sI https://www.bryanodina.com/` (fresh `Content-Length`, favicon.svg now `200`) and confirmed all 6 known aliases (found one more than expected via `vercel alias ls`: `portfolio-ud47-redcheekscoder-...vercel.app`) now point to the same latest deployment hash.
- **Going forward:** every deploy this session (and any future one) must re-point **all** aliases currently registered — run `vercel alias ls --scope redcheekscoders-projects | grep portfolio-ud47` (or `bryanodina`) to get the full current list rather than relying on a remembered subset, since the set of aliases can grow (e.g. a custom domain gets added) without every session being aware of it.
- **Files touched:** none in the repo — Vercel alias configuration only. `Portfolio/LOGS.md` (this entry).

## 2026-07-19 — Added Sunwise case study to Work section
- Added a new work card + `CASE_STUDIES` entry (`sunwise`) — a solar savings calculator web app for the "Web Apps" / "Automation" filters: AI Studio front end, formula-based estimation engine (regional sun-hours by ZIP, no external API), lead-scored admin dashboard, n8n → GHL/Telegram/SMS automation, and an auto-emailed ReportLab PDF savings report.
- Card includes a "Visit site ↗" link to `https://preview-1784471587162807465.vibepreview.com/` — this is a preview URL and may expire; swap for a permanent domain when Bryan has one.
- Both provided screenshots (CDN hashes `6a5ce6341a0f048050f44689`, `6a5ce634baf5f6da40eaf7bc`) were downloaded and visually PII-checked per CLAUDE.md §9 — clean, only placeholder form values ("John Doe", "john@example.com"), so no blur applied. Image 1 (full calculator view) is the card thumbnail.
- Description/problem/workdone copy drafted by the agent from Bryan's project brief — needs Bryan's review like the other Work copy (CLAUDE.md §8 item 6).
- Files touched: `index.html`, `CLAUDE.md` (§4 card count, §5 table), `LOGS.md`.

## 2026-07-19 — Lightbox prev/next arrows (case-modal image slider)
- Per Bryan's request: viewing a case study's images full-size no longer requires closing the lightbox between images. Opening any image from a case modal now loads the whole case's image list into the lightbox, with orange circular prev/next arrow buttons on both sides (primary orange `--accent`, hover → `--accent-hover`), wrap-around navigation, ArrowLeft/ArrowRight keyboard support, and a "N / M" counter appended to the caption.
- Arrows and counter are hidden for single-image cases and the legacy `openLb()` path; smaller arrow buttons on ≤600px viewports.
- **PII fix included:** the full-size lightbox previously showed `blur:true` images (Vanguard Credits dispute letters, etc.) UNBLURRED with only a "(blurred for privacy)" caption — despite CLAUDE.md §9 describing them as blurred in the lightbox. The lightbox now applies a real 18px CSS blur per displayed image, and it follows the image as you navigate. (Blur is still CSS-only — the raw CDN URL remains unblurred, same §9 caveat as before.)
- Verified with Playwright against a local server: 18/18 checks passed (arrows show/hide, wrap-around both directions, keyboard nav, counter, blur applied/removed per image, Escape close, work-filter regression, no JS errors) plus a visual screenshot of the arrows.
- Files touched: `index.html` (lightbox CSS/HTML/JS + `openCase()` wiring), `CLAUDE.md` (§9, §10), `LOGS.md`.

## 2026-07-19 — Federal Barbers: automation category + 6 workflow screenshots
- Added `automation` to the Federal Barbers work card's `data-category` and an "Automation" tag pill, so it now appears under the Automation filter.
- Added Bryan's 6 new GHL workflow screenshots to the `federalbarbers` case study (after the existing website capture): 001 opt-in → tag → pipeline opportunity, 002 appointment confirmation + 24h/1h/5min reminder emails, 003 customer showed, 004 customer no-show (task + pipeline update + admin notification), 005 done service → thank-you + PDF gift email, 006 30-day rebooking reminder.
- All 6 downloaded and visually PII-checked per CLAUDE.md §9 — clean (GHL workflow builders, only Bryan's own avatar visible), no blur needed.
- Expanded the modal's description/problem/workdone copy to cover the appointment-lifecycle automations (agent-drafted — needs Bryan's review like the rest, §8 item 6).
- Appended the 6 links to `Automations.txt` ("Federal Barbers 1–6") per Bryan's request, matching the file's existing format.
- Files touched: `index.html`, `Automations.txt`, `CLAUDE.md` (§5 table), `LOGS.md`.

## 2026-07-19 — Work-card tag pills and "Learn more" shrunk (per Bryan)
- `.work-tag`: font-size 11.5px → 9.2px (80%), padding scaled to match (4px 9px → 3px 7px).
- `.work-case-btn` ("Learn more →"): was effectively 16px / weight 500 (its `font:inherit` was overriding `.work-link`'s 13px/700) — now explicitly 11.2px (70%) / weight 400. The "Visit site ↗" links (plain `.work-link`) are untouched at 13px/700, per Bryan's request targeting only "Learn more".
- Verified computed styles in a headless browser before and after, plus a visual card screenshot.
- Files touched: `index.html`, `LOGS.md`.

## 2026-07-19 — Fixed hero CTA cut off on 13" / short-height screens
- Bryan reported the "Book a quick call" / "See the work" CTA buttons weren't visible in a 13" screen simulation. Reproduced exactly with a headless browser at ~1037x610: the headline's `clamp(34px,5vw,52px)` sized off full viewport width and grew to 4 lines (232px tall) while the 2-column grid (`min-width:920px`) narrowed the text column, pushing the CTA to top=623px in a 610px-tall viewport — fully below the fold.
- Fix (CSS only, `index.html`, added right after `.hero-stat span`): two new media queries — `max-width:1180px` caps the headline to `clamp(34px,4.1vw,44px)` and trims hero padding/margins for the 2-col squeeze band (920–1180px); `max-height:760px` independently compresses vertical spacing for short viewports regardless of width. No markup changes; desktop `min-width:920px` grid and mobile `clamp()` floor untouched.
- Verified headless across 7 viewports (1037x610 user case, 1024x768, 1280x800, 1440x900, 1500x950 desktop, 390x844 mobile, 768x1024 tablet) — CTA now renders above the fold at every size, desktop unchanged (H1 stays 52px/3 lines, CTA bottom=616 same as before), mobile unchanged (H1 34px). Also visually confirmed via screenshots at 1037x610 and 390x844 — balanced, not cramped.
- Files touched: `index.html`, `CLAUDE.md` (§4), `LOGS.md`.

## 2026-07-19 — Replaced card hover glow with hero-style electric border pulse
- Per Bryan's request: the orange box-shadow pulse (`@keyframes card-glow`) on hover across all 7 card types (service, badge, work, flow, video, testimonial, process-step cards) is replaced with the same "electricity flowing in a wire" language as the hero flow diagram — a glowing dash traveling the card's own rounded border.
- Implementation: a small inline SVG (`<svg class="card-bolt">`, one `<rect pathLength="200" rx="10">`) is injected into every matching card by JS on load; on `:hover` it fades in and the rect animates `stroke-dashoffset` around a normalized 200-unit perimeter (`bolt-travel`, 2.6s linear infinite), giving a `#FFD9A8` comet with a CSS `drop-shadow` glow that traces the border clockwise. Existing hover lift + accent border-color are untouched; `pointer-events:none` keeps click targets working.
- `@keyframes card-glow` and its box-shadow removed entirely; the old reduced-motion rule (which only existed to disable `card-glow`) now just hides `.card-bolt`.
- Verified with Playwright: 18/18 automated checks passed (every card type gets a bolt overlay, opacity/animation correctly toggle on hover/unhover for work/badge/flow cards, no `card-glow` left in rendered HTML, case-modal click regression still works, `prefers-reduced-motion` hides the overlay, no JS errors). Also visually confirmed with deterministic dash-offset screenshots showing the comet actually traveling around all sides of the border (not stuck in one spot).
- Files touched: `index.html` (CSS + inline JS), `CLAUDE.md` (§6), `LOGS.md`.

## 2026-07-19 — Fixed electric border pulse: moved outside the card, +1px stroke weight
- Bryan reported the new electricity hover effect (added earlier today) wasn't visible on cards with thumbnail images. Root cause: `.work-card`, `.flow-card`, and `.video-card` use `overflow:hidden` to round their edge-to-edge thumbnails, which clipped the `.card-bolt` glow (it was drawn just *inside* the border, right where the image/video sits).
- Fix: removed `overflow:hidden` from those 3 card rules and moved the corner-rounding directly onto the thumbnail elements instead (`.work-thumb`, `.flow-card img`, `.video-card video` → `border-radius:var(--radius) var(--radius) 0 0`) — visually identical rounded thumbnails, but the card no longer clips its own children.
- Repositioned `.card-bolt rect` to trace 4px **outside** the card's border (was inset 1.25px inside it) and bumped `stroke-width` from 2.5 → 3.5 (+1px) per Bryan's request, so the comet is now a clearly separated glowing outline around every card, not overlapping card content.
- Re-verified with Playwright: same 18/18 checks still pass post-fix (bolt overlay present on all 7 card types, opacity/animation toggle correctly, case-modal click regression, reduced-motion hides it, no JS errors). Visually confirmed via cropped screenshots with margin around work/flow/badge cards showing the glow clearly outside the border, a corner close-up confirming the thumbnail's rounded top-left corner still renders cleanly with `overflow:hidden` removed, and deterministic dash-offset frames confirming the outside trace still travels all the way around the perimeter.
- Files touched: `index.html`, `CLAUDE.md` (§6), `LOGS.md`.

## 2026-07-22 — Added "N8N" Work filter + n8n Academy case-study card
- Bryan asked for a new Work-section category showcasing his completed n8n Academy certification track, using 7 screenshots he supplied of his actual course-build workflows.
- Added a new `n8n` filter button (`data-filter="n8n"`, labeled "N8N") to `.filters`, and a new 18th work card (`data-category="n8n"`, `data-case="n8nacademy"`) at the end of `#workGrid` — per Bryan's choice, only this new card is tagged `n8n`; existing n8n-adjacent cards (Sunwise, Level Up Academy) were left untagged. Thumbnail is image 3 of the 7 (Bryan's pick), an annotated "Generating Reports" workflow build.
- Added a matching `n8nacademy` entry to `CASE_STUDIES` with all 7 images (`im()` calls, all `blur:false`) and description/problem/workdone copy.
- Copy sourced from the 4 n8n badge pages already linked in the Certifications section (`badges.n8n.io` — QS101 Quickstart, n8n101 Essentials, n8n102 Integrations, n8n103 In Practice). The badge site returns 403 to normal fetches; content was retrieved by requesting with a Googlebot user-agent (the site prerenders for crawlers), which returned each badge's course name, skills list, and earning criteria (70%+ graded assessments validated via the n8n Academy assessment API).
- PII-checked the 7 supplied images per `CLAUDE.md` §9 convention: no third-party PII found. One flag — image 6 (sales data pipeline) has a sticky note showing Bryan's own email (`johnbryanodina@gmail.com`); left unblurred as it's his own public contact info, but called out to him rather than assumed.
- Verified: extracted and syntax-checked the inline `<script>` block with `node` (`new Function(...)`) — no errors. Filter/case-modal JS required no changes since both are already data-driven off `data-category`/`data-case`/`CASE_STUDIES`.
- Card and modal copy (description/problem/workdone/tags) is agent-drafted from the badge pages, same as all other case studies — unreviewed by Bryan until confirmed (see `CLAUDE.md` §8.6).
- Files touched: `index.html`, `CLAUDE.md` (§4, §5), `LOGS.md`.
- Still needed from Bryan: confirm the drafted copy reads right, confirm image 6's email should stay unblurred, and (per repo Deploy Rules) this session still needs to commit/push/deploy — not yet done as of this entry.

## 2026-07-19 — Electric border pulse: aligned to the actual border, slowed 40%
- Bryan felt the border glow (moved 4px outside the card in the previous fix) read as a disconnected floating outline rather than the border itself lighting up, and wanted the travel speed reduced.
- Repositioned `.card-bolt rect`: `x/y:-0.5px`, `width/height:calc(100% + 1px)`, `rx:10px` (matching the card's own `--radius` exactly) — the stroke is now centered directly on the card's real 1px border, so the glow visually traces/coincides with the border rather than floating in a separate ring around it. (`overflow:hidden` stays removed from `.work-card`/`.flow-card`/`.video-card` from the prior fix, so nothing clips.)
- Slowed `bolt-travel` animation duration 2.6s → 3.64s (2.6 × 1.4, i.e. 40% slower) across all 7 card types; stroke-width unchanged at 3.5.
- Re-verified with Playwright: same 18/18 checks still pass, plus a direct `animationDuration` read confirming `3.64s`. Visually confirmed via cropped screenshot + corner close-up showing the glow now hugging the border line itself instead of floating outside it.
- Files touched: `index.html`, `CLAUDE.md` (§6), `LOGS.md`.

## 2026-07-22 — Added 6 workflow screenshots to Level Up Academy case study
- Appended 6 new `im(...)` entries to the `lua` case study's `images` array (11 → 17), documenting LUA's gamified-quest automation system: LUA 01 Chosen Character (gender-branch onboarding), LUA 02.01/02.02 Quest reward item delivery (random item lookup + email), LUA 03 Leveling Up, LUA 05 Daily Check-in, LUA 06 Reset Check-in Streak.
- All 6 downloaded and visually PII-checked per CLAUDE.md §9 before adding — clean GHL workflow-builder views, only Bryan's own avatar visible, no third-party data — no blur applied.
- No markup/CSS/copy changes needed; the existing modal grid + lightbox gallery/prev-next arrows (added 2026-07-19) already handle any image count.
- Verified with Playwright: modal now renders 17 thumbnails, gallery nav (arrows + counter) correctly reaches the new images at their true positions (12/17, 13/17), no JS errors.

## 2026-07-27 — Work section made fully data-driven; added "Claude Projects" category + Affiliate Signup Alerts case study
- **Root cause of the ask:** Bryan wants to add new projects under a "Claude Projects" category going forward and asked for the code to be prepared so future additions are easy. The Work section previously had zero data-driven rendering — all 18 `.work-card` elements and all 10 `.filter-btn` elements were hand-written static HTML, with a separate matching `CASE_STUDIES` JS entry per card. Adding one project meant editing three disconnected places by hand with silent failure on a slug mismatch (`openCase` no-ops on a missing key).
- **Refactor:** introduced `WORK_CATEGORIES` and `WORK_PROJECTS` arrays at the very top of the inline `<script>` (must run before the `.reveal`/`.card-bolt`/filter-handler `querySelectorAll` snapshots later in the same script, since those need the generated elements to already exist in the DOM). A `renderWork()` IIFE builds the filter buttons into `#workFilters` and all work cards into `#workGrid` from this data; `CASE_STUDIES` is now `Object.fromEntries(WORK_PROJECTS.map(p => [p.slug, p]))` merged with an `Object.assign` of the 10 Process Design (`pd-*`) entries, which still have no work-card and are unchanged. All 18 existing projects were migrated field-for-field (categories, thumbnail + `data-thumbs` overrides, tags, external link + label, description/problem/workdone, images) — no content was altered. **Adding a new project going forward is one appended object in `WORK_PROJECTS`; adding a new filter category is one appended object in `WORK_CATEGORIES` plus tagging a project with the new slug** — no HTML editing required for either.
- **New project added:** "Affiliate Signup Alerts" (`slug: affiliatenotif`, industry "Community / SaaS"), tagged with a new `claude` category/filter button labeled "Claude Projects" (mirrors how `n8nacademy` carries only the `n8n` tag — not cross-tagged into `automation`). Documents a FirstPromoter `lead_signup` webhook → Vercel serverless function → Discord embed notifier, built with Claude Code (source project outside this repo: `discord_freetrial`, private GitHub repo). 5 images supplied by Bryan: architecture diagram (used as thumbnail), the live Discord channel result, the Vercel deployment dashboard, VS Code + Claude Code mid-session, and the GitHub repo page.
- **PII check (CLAUDE.md §9):** the Discord-channel screenshot (image 2 of 5) shows two real subscribers' Gmail addresses from actual free-trial signups — confirmed visually, not assumed. Flagged to Bryan; he confirmed the site's standard treatment (`blur:true`, same CSS blur + "blurred for privacy" overlay used for the Vanguard Credits dispute letters). The other 4 images were checked and are clean (the VS Code screenshot shows only `process.env.DISCORD_WEBHOOK_URL`, no live credential value).
- **Verification performed:** `node --check` on the extracted inline script (syntax); executed the full data-definition portion of the script under Node with stubbed `document`/`window` globals to confirm `WORK_PROJECTS.length === 19`, `WORK_CATEGORIES.length === 11`, `CASE_STUDIES` has 29 keys (19 + 10 `pd-*`), no project missing `images`, no duplicate slugs, and every non-`all` category has at least one project tagged to it; ran `renderWork()`'s actual template-literal logic and diffed the generated HTML byte-for-byte against the original static markup for representative cards (squirrel/AEMR's `data-thumbs` JSON, deanna/lua's non-standard link labels, the new affiliatenotif card) — all matched exactly. Full browser/Playwright verification (visual reveal-on-scroll, hover electric-border, live click-through of filters and modals) was **not** run in this session — no Playwright install or browser driver was available without a fresh package install, so this was static/logical verification only, not an in-browser check.
- Files touched: `index.html` (HTML: emptied `.filters`/`#workGrid` into two empty containers with an explanatory comment; JS: new data + render block, collapsed old `CASE_STUDIES` literal), `CLAUDE.md` (§4, §5, §9, §11 — documents the new data-driven architecture and "how to add a project" pattern), `LOGS.md`.
- **Still needed from Bryan:** visually confirm the site in a real browser after deploy (this session verified data/HTML output only, not live rendering); review the drafted description/problem/workdone copy for Affiliate Signup Alerts (same unreviewed-copy caveat as all other case studies, CLAUDE.md §8.6); optionally re-upload a redacted version of the Discord-channel screenshot if the CSS-only blur (raw CDN URL still serves the unblurred original) isn't a strong enough guarantee for those two real subscribers' emails.
- Files touched: `index.html`, `CLAUDE.md` (§5), `LOGS.md`.

## 2026-07-27 — Tagged Vanguard Credits as a Claude Project
- Bryan asked for Vanguard Credits to also show up under the new "Claude Projects" filter (added earlier today, see the entry above). Added `'claude'` to its `categories` array in `WORK_PROJECTS` (`index.html`) — one-line change, no HTML/CASE_STUDIES edit needed since the Work section is now data-driven.
- Vanguard Credits now has 4 categories: `ai`, `apps`, `automation`, `claude`. It shows in both the AI/Web Apps/Automation filters (unchanged) and the new Claude Projects filter.
- Files touched: `index.html`, `CLAUDE.md` (§5), `LOGS.md`.

## 2026-07-27 — Contact form wired to Telegram bot
- **Root cause of the ask:** the `#contact` form (`CLAUDE.md` §8 Known Gap #2) had no `action`/`method`/`name` attributes and zero JS wiring — submitting it did a native GET to the same URL and silently discarded the message. Bryan wants every inquiry pushed to his own Telegram bot instead.
- **New backend:** `api/contact.js` (`POST /api/contact`) + `api/_lib/telegram.js` (`sendTelegramMessage(text)`, `escapeHtml(s)`). Follows the exact conventions of the existing booking backend (`api/book.js`/`api/_lib/ghl.js`): ESM `export default async function handler(req,res)`, module-local `requireEnv()`, plain global `fetch` with no SDK/dependency added, `{success,error,message}` JSON response shape, `console.error` on failure for `vercel logs` visibility. New env vars `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` (already in Bryan's hand — no BotFather setup needed this session), documented in `.env.example`.
- **Decision (Bryan's):** Telegram only — no GHL contact is created for these inquiries, unlike the booking flow's dual write. `upsertContact()` in `api/_lib/ghl.js` was left untouched/unused here per his explicit choice.
- **Validation/spam guard (Bryan's choice):** honeypot only, no rate limiting. A hidden, off-screen, `tabindex="-1"` `#cf-company` input in the form — if filled, the endpoint silently returns `200 {success:true}` without sending anything (no signal given to the bot). Real fields (`name`,`email`,`question` required, `whatsapp` optional) are validated with the same `EMAIL_RE` used in `api/book.js` plus per-field length caps; errors return `400` with codes `missing_fields`/`invalid_email`/`too_long`.
- **HTML-escaping is load-bearing, not cosmetic:** the Telegram message uses `parse_mode:'HTML'` for bold labels, so every visitor-supplied value is passed through `escapeHtml()` first — an unescaped `<` would otherwise make Telegram reject the entire send with a 400. Verified directly (see testing below).
- **Frontend:** `index.html`'s `#contactForm` gained the honeypot field, `required` on name/email/question (previously nothing was required), `id`s on the submit button and two new status lines (`#contactError`, reusing the existing `.booking-error` CSS rule; `#contactSuccess`, styled inline in `--accent-2`). A new IIFE appended after the booking widget's in the inline `<script>` submits via `fetch('/api/contact', ...)`, mirroring the booking form's submit-handler pattern (disable+relabel button, hide/show status line, `catch`→"Network error", `finally`→re-enable). Success calls `form.reset()` rather than swapping panels, so the form stays usable for a follow-up message.
- **Testing performed:** `node --check` on both new files. Offline test (throwaway script in the session scratchpad, not committed to the repo) stubbed env vars and monkey-patched `globalThis.fetch` to capture the outgoing request instead of sending it — confirmed: a valid payload formats correctly and escapes a `<script>` tag and an `&` in both the name and question fields; a honeypot-filled payload short-circuits with zero fetch calls; missing-fields, invalid-email, too-long, and wrong-method cases all return the expected status/error code. All 6 cases passed.
- **Still needed from Bryan:** set `TELEGRAM_BOT_TOKEN` and `TELEGRAM_CHAT_ID` in Vercel Project Settings → Environment Variables (production) before this works live — the agent never had/needs the actual token value. Until those are set, `/api/contact` will 500 on `requireEnv`. Once set and deployed, send a real test inquiry through the live site to confirm the notification lands in Telegram.
- Files touched: `api/contact.js` (new), `api/_lib/telegram.js` (new), `index.html`, `.env.example`, `CLAUDE.md` (§4, §8, new §14), `LOGS.md`.

## 2026-07-28 — Added "Business Automation Systems" card under the N8N category
- Bryan supplied 10 n8n workflow screenshots plus a written description of each, and asked for them added under the N8N filter (which had exactly one card, n8n Academy, until now). Decisions confirmed with him up front: **one card** holding all 10 images (not 10 separate cards); his 10 descriptions **folded into the 3 existing modal fields** (Description/Problem/What Was Done) rather than adding a new modal section or a per-image caption system (the `im()` helper only carries `src`/`blur` today — no caption field exists, and he chose not to add one); framed as **demonstration/template builds**, not real named or unnamed clients (the copy he wrote reads generically — "a church," "a clinic," "the average small business" — so the card says so explicitly rather than implying otherwise); titled "Business Automation Systems" / industry "Multi-Industry".
- **PII check performed on all 10 images before wiring anything in** (per `CLAUDE.md` §9's standing instruction to check every new screenshot, and direct precedent — n8n Academy image 6 previously showed Bryan's own email in a sticky note). Downloaded and visually reviewed all 10: clean across the board — no third-party names, emails, phones, or live credential values. Node fields that would normally carry secrets (webhook URLs, form IDs) show generic `REPLACE_WITH_...` placeholders, not real values — which also reinforced the "these are template builds" framing, since a live client system wouldn't have placeholder text sitting in its nodes. No image needed `blur:true`.
- **Added one entry to `WORK_PROJECTS`** (`slug:'n8nsystems'`) per the existing data-driven pattern (§11) — no HTML editing, no separate `CASE_STUDIES` entry. `categories:['n8n']`, thumbnail is image 01 (Missed-Call Text-Back & Speed-to-Lead, most broadly relatable use case), all 10 images in Bryan's original 01–10 order, none blurred. Description/problem copy synthesizes the shared thread across all 10 write-ups (follow-up that only happens if someone remembers) using the concrete stats Bryan provided (78% of buyers go with whoever responds first; ~$17,500 owed to the average small business; 30–60% no-show reduction; 8–14 month average client retention). `workdone` is a single numbered enumeration of all 10 workflows.
- **CSS change:** `.case-section p` gained `white-space:pre-line` so the numbered `workdone` list renders as actual line breaks instead of one run-on paragraph. Checked against all other 29 case studies' `description`/`problem`/`workdone` strings — none contain a newline, so this is a no-op for every other card; only Business Automation Systems is affected.
- **Verification performed:** all 10 CDN image URLs confirmed 200 via curl before use. `node --check` on the extracted inline script. Ran the script's data-definition portion under Node (`vm` module, stubbed `document`/`window`) and asserted: `WORK_PROJECTS.length === 20`, no duplicate slugs, `CASE_STUDIES` has 30 keys (20 + 10 `pd-*`), the new entry has every required field with `images.length === 10` in the correct order and all `blur:false`, `n8n` category now matches exactly 2 projects (`n8nacademy`, `n8nsystems`), and every other category still has at least one project. Also ran `renderWork()`'s actual template logic against a stateful DOM stub and confirmed the generated card HTML contains `data-case="n8nsystems"`, `data-category="n8n"`, the correct thumbnail URL, and correctly-escaped tags. Full browser/visual verification (filter click-through, modal open, lightbox gallery paging through all 10 images, the pre-line list actually breaking across lines) was **not** run in this session — static/logical verification only, same caveat as the 2026-07-27 data-driven refactor.
- **Still needed from Bryan:** review the drafted description/problem/workdone copy (agent-written and unreviewed, same standing caveat as all other case studies — `CLAUDE.md` §8.6); visually confirm the card/modal in a real browser after deploy; confirm image 01 is an acceptable thumbnail choice (chosen for broad relatability, not explicitly requested).
- Files touched: `index.html` (new `WORK_PROJECTS` entry, `.case-section p` CSS), `CLAUDE.md` (§4, §5), `LOGS.md`.

## 2026-07-31 — Added Wolfpack Basecamp and SSSGRP Litigation Support case studies
- Bryan supplied 2 Skool community screenshots + 1 website capture labeled "Community tag" / "Website view tag", a funnel link (wolfpackbasecamp.com/skool-offer) to source description copy from, and separately a single landing-page screenshot for a new project, "SSSGRP Litigation Landing page." His "tag" labels mapped directly onto the existing `thumbAlt` per-category thumbnail-swap mechanism (§11) rather than needing any new filter category — resolved by inference, then confirmed correct by the successful render check (see below).
- **Clarified with Bryan before writing any copy** (three questions, all answered): (1) his role with Wolfpack Basecamp — confirmed he built the website, GHL CRM, and marketing automation behind it (the funnel sells those as part of the membership, Departments 3-5 in the Skool classroom), so the card is framed as client delivery work, not just membership; (2) the SSSGRP screenshot's lead-capture form shows Bryan's own real name, phone number, and email typed in as test data — flagged per `CLAUDE.md` §9's standing rule to check before publishing, since it's more than the email-only precedent from n8n Academy (this includes a real phone number); Bryan said leave it unblurred; (3) the Wolfpack community-feed screenshot shows a real third-party member's name/avatar ("Thomas Thiel") on a leaderboard sidebar; Bryan said leave it unblurred too; (4) confirmed SSSGRP is real client work, not a demo build, and provided a GHL preview URL for the live site.
- **Wolfpack Basecamp** (`slug:'wolfpackbasecamp'`, categories `web`/`automation`/`courses`): default thumbnail is the website/funnel capture; `thumbAlt.courses` swaps to the Skool community-feed screenshot when the "Courses & Community" filter is active (the mechanism Bryan's "Community tag" / "Website view tag" labels were describing). All 3 images unblurred per his explicit calls above. `link` set to the live funnel URL (`wolfpackbasecamp.com/skool-offer`, confirmed reachable — this is the same URL `CLAUDE.md`'s design-system section already cites as the source of the site's color palette). Description/problem/workdone copy drafted from that funnel page (fetched successfully via WebFetch — headline, offer stack, pricing, and value proposition) plus the supplied screenshots.
- **SSSGRP Litigation Support** (`slug:'sssgrp'`, category `web` only, single image): a landing page for a use-of-force/security/law-enforcement expert-witness litigation-support practice. Bryan provided a GHL preview URL (`sites.leadconnectorhq.com/preview/...`) for sourcing copy — it returned `403 Forbidden` both via WebFetch and via curl with a Googlebot user-agent (the n8n Academy workaround from 2026-07-22 didn't apply here; this looks like an access-gated preview, not a bot-detection block). Copy was instead drafted directly from the supplied screenshot, which already contained the full page copy (headline, four core practice areas, the featured expert's bio and credential stats, the four-step engagement process) — cropped and re-read at higher resolution to confirm the lead-form contents before deciding on the PII question above. `link:null` since the preview URL's public accessibility was never confirmed; worth swapping in a real domain if/when Bryan has one live.
- **Verification performed:** all 4 CDN image URLs confirmed 200 via curl before use. `node --check` on the extracted inline script. Ran the data-definition portion under Node (`vm` module, stubbed `document`/`window`) and asserted: `WORK_PROJECTS.length === 22`, no duplicate slugs, `CASE_STUDIES` has 32 keys (22 + 10 `pd-*`), both new entries have every required field, `wolfpackbasecamp.images.length === 3` in the correct order with `thumbAlt.courses` pointing at the right hash, `sssgrp.images.length === 1`, both projects' images all `blur:false` as decided, and every filter category still has at least one project (`web` now 11, `automation` now 14, `courses` now 4). Also ran `renderWork()`'s actual template logic against a stateful DOM stub and confirmed both generated cards' HTML: `data-case`/`data-category` attributes correct, and — specifically for Wolfpack — the `data-thumbs` JSON attribute correctly carries the `courses` override URL. Full browser/visual verification (filter click-through, modal open, lightbox paging, the courses-filter thumbnail actually swapping on hover/click) was **not** run this session — static/logical verification only, same standing caveat as the two prior data-driven-refactor sessions.
- **Still needed from Bryan:** review the drafted description/problem/workdone copy for both new cards (unreviewed, standing caveat per `CLAUDE.md` §8.6); visually confirm the courses-filter thumbnail swap works as expected in a real browser; get a permanent/public SSSGRP URL if he wants a "Visit site" link added later.
- Files touched: `index.html` (2 new `WORK_PROJECTS` entries), `CLAUDE.md` (§4, §5, §9), `LOGS.md`.

## 2026-08-01 — Added second page `/keyland-compliance-group` (CTO application portfolio summary)
- **Why:** Bryan is applying for a CTO / Technology Director role at Keyland Compliance Group and they asked him to send a link summarizing the systems he's built, across five categories (websites, CRM systems, customer portals, SaaS platforms, mobile/web applications), each answering five questions: business problem solved, his specific role, solo vs. team, technologies, and approximate users. **This is the first time the site has had more than one page** — see the new `CLAUDE.md` §15.
- **Research done first, and it materially shaped the page:** found Keyland's actual live job posting (OnlineJobs.ph ID 1699693, posted 2026-07-31). They are a **US trucking/FMCSA compliance company** — USDOT authority setup, BOC-3 filings, drug & alcohol consortium (49 CFR Part 40/382), safety audit prep, trip permits, insurance — *not* financial or healthcare compliance, which is what the name alone would suggest. The posting explicitly names **GoHighLevel** alongside HubSpot/Zoho/Salesforce under preferred experience, which is Bryan's single strongest differentiator. Its responsibilities map nearly 1:1 onto his portfolio (CRM build/customization, website management, customer portals, internal business apps, AI & workflow automation, document processing, compliance tracking automation), and it describes a hands-on solo technical function with no direct reports — which makes his "solo on everything" answer an asset rather than a gap. Also searched a dozen domain variants plus LinkedIn/Crunchbase/registries and found **no public web presence** for the company; this was deliberately NOT asserted anywhere on the page (a site may exist under a name not found), but it's why the page leads with shipped, linkable artifacts instead of architecture philosophy.
- **Routing decision:** added a targeted `rewrites` entry to `vercel.json` rather than enabling `cleanUrls: true`. Verified empirically before changing anything that `cleanUrls` was off (`/index` returned 404 while `/index.html` returned 200) and confirmed against Vercel's docs that it defaults to `false`. Enabling it would have generated site-wide 308s making the currently-working `/index.html` redirect to `/` — no benefit here, real (if minor) blast radius. The rewrite touches nothing else; the existing `functions` key was preserved.
- **New file `keyland-compliance-group.html`:** self-contained, no build step, visually identical to the main site. Contains a **copy** of `index.html`'s design tokens and shared component CSS — there is no shared stylesheet to import, so the two pages can drift; documented prominently in §15. Simplified header (logo → `/`, back link, `/#book` CTA) because `index.html`'s nav is entirely bare `#hash` anchors that would silently no-op on a standalone page. Footer copied verbatim (portable). Only JS is the `.reveal` IntersectionObserver. First page on the site with a `rel="canonical"` and OG/Twitter tags.
- **Content:** intro + credentials, a 15-row at-a-glance table, then five sections matching Keyland's five categories, each system as a card with a fixed five-row spec block (Problem / My role / Solo or team / Technologies / Scale). Closes with a "how this maps to Keyland" grid tying existing work to FMCSA-adjacent problems — compliance deadline tracking (Vanguard's 17-rule engine + 30/35-day rounds ≈ UCR/IFTA/MCS-150/Clearinghouse cycles), document processing (4-format parser + bureau letter generation ≈ CDL/insurance-cert/MCS-150 intake), GoHighLevel, A2P 10DLC, self-service, AI support, payments, internal dashboards — plus an explicit, non-defensive statement that he has no prior transportation-compliance experience.
- **Facts confirmed with Bryan because they are NOT inferable from the portfolio** (asked rather than guessed, since a wrong stack claim would be caught in a technical interview): stacks — NextLevel = Flask + Supabase, Woop = Flask + Supabase, AutoQuote = **GHL** AI Studio, and the ambiguous "AI Studio" in the Sunwise/Charity Lift copy resolved to **GoHighLevel** AI Studio (not Google's); user counts — NextLevel 52, AutoQuote 26 and counting, Vanguard Credits 2 (single-company), Sunwise 1 (client-customized); and that he was **solo end-to-end on every system**, with the 414 Project CTO title being technical/strategic rather than people management.
- **Fact audit before deploy** (the highest-risk part — this page carries his name into a hiring decision): every claim was grepped back to its source in `WORK_PROJECTS`. Confirmed verbatim: Federal Barbers' 24h/1h/5min reminders, AEMR's 6,030 opportunities / 3,011 sends / 31.68% open rate / €7,450, Vanguard's 4 report formats / 17 rules / 30-35 day rounds / Flask+Railway+Supabase, LUA's 8.7K members and 360 sub-accounts, Sunwise's sun-hours-by-ZIP and ReportLab, Wolfpack's eleven departments. Corrected two counts by checking rather than assuming: **3** A2P 10DLC clients (the three `compliance`-tagged cards — Genesis Credit's copy words it differently from the other two, so a naive grep undercounted) and **4** n8n badges (a fifth `badges.n8n.io` URL exists but is the wallet profile). Deliberately sourced nothing from the decorative `aria-hidden` keyword marquee — React/Next/Node/Twilio/OpenAI/Postgres/Firebase appear only there and are attributable to no project.
- **Verification performed:** custom HTMLParser-based tag-nesting check on the new page (zero unclosed tags, zero mismatches); `node --check` on the extracted inline script; a programmatic diff of the two `:root` blocks confirming all **15 design tokens match `index.html` exactly** (no missing tokens, no value mismatches). Post-deploy: routing checks on all four URL forms plus regression checks that `/index.html` still returns 200 (not 308) and the `/api/*` functions still respond, proving the `vercel.json` edit didn't disturb them. **Browser/visual verification was not run** — Playwright is not installed and adding it would put `node_modules` into a repo that is deliberately zero-dependency; same standing caveat as recent sessions.
- **Nav link:** added "Tech Summary" → `/keyland-compliance-group` to both the desktop `.nav-links` and `.mobile-menu` in `index.html`. Bryan explicitly chose public + linked over unlisted/noindex. Label kept neutral rather than naming Keyland, since every other visitor and prospective client sees it — flagged to him that a nav item pointing at a company-specific application page is publicly visible, and that removing it is a two-line revert.
- **Still needed from Bryan:** read the page end to end before sending the link — it makes factual claims about his own work in a hiring context. Also note the job posting requires the application itself to **begin with "KEYLAND CTO"** or it won't be considered, and separately asks for a 90-day plan, availability, and salary expectations, none of which are on this page (he chose to handle those in the application directly).
- Files touched: `keyland-compliance-group.html` (new), `vercel.json` (rewrite added), `index.html` (2 nav links), `CLAUDE.md` (§1 + new §15), `LOGS.md`.

## 2026-08-01 — Screenshot galleries on the Keyland page; three images unblurred
- **Why:** the Keyland page shipped text-only — 24 systems described, zero proof shown — and it goes to an employer who explicitly asked for "examples of websites, CRMs, applications, or systems you have built." Bryan asked for a way for visitors to see the screenshots, and separately asked to unblur the site's blurred images, describing it as temporary.
- **Galleries:** ported the lightbox from `index.html` (shell, CSS, `renderLb`/`openLbGallery`/`stepLb`/`closeLb`, and the close/prev/next/backdrop/Escape/arrow listeners) and added a `GALLERIES` map + a `data-gallery` attribute on each `.sys-card`, with a render loop that injects a thumbnail strip (max 6 tiles, `+N` overlay when there are more). **23 of 24 cards** got a gallery; `bryanodina.com` is the exception since no screenshots of it exist. Adding a gallery in future = one `GALLERIES` key + one attribute.
- **Two deliberate deviations when porting the lightbox**, both necessary: the Escape handler calls only `closeLb()` (index.html's version also calls `closeCase()`, which doesn't exist on this page and would throw `ReferenceError` on every Escape press), and the `[data-full]` querySelectorAll loop was dropped. Verified afterward that the only remaining mentions of either are inside the explanatory comment, not executable code.
- **Non-1:1 card mappings resolved rather than guessed:** `wisdomchurch` merges the two ministries that share an automation system (11 images); `additional` combines SSSGRP + Funded Biz + Charity Lift + DeAnna Crawford; `lifecycle` pulls automation shots from the six clients that card names; `ghlscale` reuses the Level Up Academy set since the 360-sub-account figure comes from there. **AutoQuote:** `CLAUDE.md` §5 says 5 of Meritex's 9 images are AutoQuote but not which 5 — downloaded and viewed all nine and confirmed images **2–6** are the car-insurance quote flow (1 is the corporate homepage, 7–9 are review automation), rather than slicing on an assumption.
- **Unblurring:** Woop #3 (Bryan's own address/phone), NextLevel #5 (other users' emails), and Affiliate Signup Alerts #2 (two subscribers' Gmails) were unblurred in `index.html` as requested — `blur:true` count went 11 → 8.
- **The 8 Vanguard Credits dispute letters were NOT unblurred, and this was communicated, not silently skipped.** Those show a real consumer's full name, DOB, home address and partial SSN — third-party identity-theft-grade data, on a page being sent to a compliance-industry employer. Bryan was told plainly and asked to reconsider once; he reaffirmed, and the decision was still to hold, with the reasoning recorded in `CLAUDE.md` §9. **Offered alternative, still open:** hard-redact the letters with PIL (black boxes burned into the actual pixels, committed to the repo, served from the site) — that would let him show real letter output and is strictly safer than today, since the current blur is CSS-only and the raw CDN URLs already serve fully readable originals. Vanguard's two cards therefore show only its PII-free website capture for now.
- **Verification performed:** cross-checked all **120 unique image hashes** on the Keyland page against `index.html` — every one valid, none malformed (a typo'd hash renders a silently broken tile, so this was a real check). Blur audit: exactly 8 `blur:true` remain in `index.html`, all Vanguard; 0 on the Keyland page; each of the three unblurred hashes confirmed no longer carrying `true`. HTMLParser tag-nesting check (zero unclosed/mismatched), `node --check` on the extracted inline script, `:root` token parity re-confirmed (15/15 still match `index.html`), and `closeCase`/`data-full` confirmed absent from executable code. Also executed the render loop under Node against the real card list parsed out of the HTML: **23/23 tagged cards produce a strip, zero empty galleries**, counts per card as expected. Browser click-through not run — Playwright isn't installed and adding it would put `node_modules` into a deliberately zero-dependency repo.
- **Still needed from Bryan:** click through the galleries in a real browser (thumbnails, lightbox, prev/next, Escape) and confirm the main portfolio's case modals still behave after the unblur edits. Also decide whether he wants the hard-redacted Vanguard letters — one word and they get built.
- Files touched: `keyland-compliance-group.html` (lightbox + galleries + CSS), `index.html` (3 unblurs), `CLAUDE.md` (§9 rewritten to match reality, §15 extended), `LOGS.md`.

## 2026-08-01 — Vanguard: all 9 images via hard redaction; CRM/SaaS recategorisation
- **Two asks:** show all 9 Vanguard Credits images unblurred on the Keyland page (only 1 was showing), and recategorise — Vanguard into *SaaS Platforms* only, with Genesis Credit and AEMR **also** appearing under *CRM systems*.
- **Inspected all 9 Vanguard images rather than trusting this repo's own notes** — and that mattered. `CLAUDE.md` §9 described "a real consumer's full name, DOB, home address, partial SSN"; the actual contents were both better and worse than that. **Four images are completely clean** (marketing site, login screen, upload screen, and a VS Code shot of the Flask codebase + file tree + architecture write-up — the strongest single image in the set for a CTO application). **Four contain identity data** — and image 9 carries a full name, a complete Chicago street address, DOB, plus form fields for SSN, client email and Google Drive links to Government ID / Proof of Address / Social Security Card / FTC Identity Theft Report. Image 7 exposed a **second** client name never previously documented. **A ninth image had an unrelated problem nobody had flagged:** the Supabase dashboard shows the live project URL next to three CRITICAL "RLS Disabled in Public" advisories on `public.clients`, `public.jobs` and `public.dispute_rounds` — effectively a published map to a database of real credit data with row-level security off.
- **Resolution after three rounds of back-and-forth:** presented the actual findings, and Bryan chose hard redaction for both the identity images and the Supabase URL. This gave him exactly what he'd been asking for — all 9 images, sharp, no blur anywhere — while removing the data from the file bytes rather than hiding it behind a CSS filter that any right-click defeats. Net result is **safer than the status quo**, since the CDN originals were always one click away.
- **Redaction method (PIL):** for each image, cropped the candidate region and *viewed* it to confirm the target text before drawing anything — no guessed coordinates. 18 black rectangles across 5 files, written to a new `images/vanguard/` directory. **Verified three ways:** viewed every output end-to-end (including behind modal backdrops, where image 8's sidebar name was still legible through the app's own blur); asserted every box is solid `(0,0,0)`; and diffed each output against its original with a mask to prove **zero pixels changed outside the intended boxes** and that each box actually landed on content. Original dimensions preserved so the 4:3 thumbnail crop behaves identically to CDN images.
- **Gallery wiring:** added a `loc(path)` helper beside `im(hash)` for repo-hosted files, and the `vanguard` gallery is now 9 entries mixing 4 CDN hashes with 5 local paths, all `blur:false`. `images/vanguard/` is the **first self-hosted image directory on this site** — everything else hotlinks the CDN.
- **Recategorisation:** deleted the "Vanguard Credits pipeline" card from `#crm` and folded its substance (custom Kanban board, 30/35-day statutory-clock reminders, "CRM logic I wrote rather than configured") into the SaaS card so nothing was lost. Added Genesis Credit and AEMR cards to `#crm`, each written from the CRM angle rather than duplicating their website copy. **Trimmed the "Lifecycle & lead-scoring builds" card**, which had named AEMR and Genesis Credit — with both promoted to their own cards it would have said the same thing twice; it now covers Federal Barbers, DeAnna Crawford, Squirrel and Wolfpack, and its gallery was re-pointed to match. Updated AEMR's at-a-glance category cell and the `#crm` section intro.
- **Verification:** 123 unique CDN hashes all still valid against `index.html`; all 5 `loc()` paths confirmed present on disk; blur audit — 0 on the Keyland page, still exactly 8 in `index.html` (untouched); HTMLParser tag-nesting clean; `node --check` clean; `:root` token parity intact. Re-ran the render harness against the real card list: **25 cards, 24 with galleries, 0 empty, 0 unused gallery keys**, `vanguard` resolving to 9 images in the right order, Vanguard appearing once (SaaS), Genesis Credit twice, AEMR three times. Browser click-through still not run — Playwright isn't installed and adding it would put `node_modules` in a deliberately zero-dependency repo.
- **Still needed from Bryan:** page through all 9 Vanguard images in the lightbox and eyeball the redactions himself before sending the link. Also worth deciding whether to point `index.html`'s Vanguard entry at the same redacted copies — it currently still uses the blurred CDN originals, and the raw files remain publicly reachable at their CDN URLs regardless of what either page does. Separately: the RLS advisories in image 3 are a real finding about a live database, not just a screenshot problem.
- Files touched: `images/vanguard/*.png` (5 new), `keyland-compliance-group.html` (`loc()` helper, 9-image Vanguard gallery, CRM/SaaS restructure, at-a-glance + intro copy), `CLAUDE.md` (§9 rewritten with the real image inventory, §15 extended), `LOGS.md`. `index.html` deliberately unchanged.

## 2026-08-02 — Equalised card sizes in the "Previous Work" grid (and the Keyland cards)
- **Report:** cards in the Previous Work section (`index.html` `#work`) were visibly different sizes. Diagnosis found **three independent causes**, not one — worth recording since fixing any single one would have left the problem visible.
- **(A) Unequal widths.** `.work-industry` had `white-space:nowrap`, and `grid-template-columns:repeat(4,1fr)` resolves each `1fr` to `minmax(auto,1fr)` — a column can never shrink below its widest child's min-content width. Long industry labels ("Legal / Expert Witness Services" at 31 chars, "Business & Credit Coaching" at 26) forced their columns wider and squeezed the rest. The giveaway was that the imbalance *changed when clicking a filter*, because `.work-card.hidden{display:none}` reshuffles which project lands in which column. Fixed by switching all four breakpoints to `minmax(0,1fr)`, dropping the `nowrap`, and adding `min-width:0` to `.work-top > *` (flex items default to `min-width:auto` and would otherwise still refuse to shrink, defeating the minmax).
- **(B) Unequal heights between rows.** No `grid-auto-rows`, so each implicit row sized to its own tallest card. Measured the actual spread before choosing a fix: descriptions run **2 to 7 rendered lines** (75 chars for Charity Lift → 305 for Business Automation Systems), about 100px of variance. Fixed with a 4-line clamp on `.work-desc` (`-webkit-line-clamp:4` plus a matching `min-height:calc(1.55em * 4)` so short blurbs reserve the same block) **plus** `grid-auto-rows:1fr`. The two go together — `grid-auto-rows:1fr` alone would have inflated all 22 cards to the 7-line height. Bryan chose the clamp over padding-to-tallest; 15 of 22 descriptions fit untouched and the full text is still in each modal.
- **(C) Misaligned footers within a row.** `margin-top:auto` was on `.work-tags`, so only the tags block was bottom-anchored and everything after it floated by a variable amount — made worse by tag counts of 2-5 (wrapping to 1-2 lines) and by the optional "Visit site" link, which **7 of 22 cards don't have**. Moved the auto margin to `.work-case-btn`, the only footer element present on every card, so "Learn more →" now pins to the bottom uniformly.
- **Keyland page — same family of fix, deliberately not identical.** `.sys-grid` got `minmax(0,1fr)` and `.sys-shots` got `margin-top:auto` so the screenshot strips align across a row. Also added `margin-bottom:18px` to `.sys-spec`, because an auto margin collapses to zero on the tallest card in a row and the strip's top border would otherwise hug the prose. **Did NOT add `grid-auto-rows:1fr` there** — those cards carry long unclamped prose across five spec rows, so equalising all 25 would create large dead space, and clamping isn't an option on a page whose purpose is to be read by a hiring manager. Recorded the reasoning in `CLAUDE.md` §15 so a future session doesn't "helpfully" add it.
- **CSS-only change — no markup or JS touched.** Confirmed by re-running the render harness: `WORK_PROJECTS` still 22, 22 `.work-card` divs generated, 22 `.work-case-btn`, 22 `.work-desc` — byte-identical output from `renderWork()`.
- **Verification:** wrote a CSS-rule parser that scopes to the `<style>` block, **strips `/* comments */` first** (without that it parses comment text as selectors and reports phantom duplicate rules — cost one confusing failed run), and asserts each property landed *and* that no later rule re-declares it. Key assertion: `.work-desc` is declared **exactly once**, so nothing can silently override the clamp. All 26 checks pass. Also `node --check` on both inline scripts and `:root` token parity across the two pages. No browser check — Playwright isn't installed and adding it would put `node_modules` into a deliberately zero-dependency repo.
- **Still needed from Bryan:** view the Work grid at desktop width and **click through 2-3 category filters** — the width bug was filter-dependent, so a single static view neither exposed it originally nor fully confirms the fix.
- Files touched: `index.html` (`.work-grid`, `.work-top`, `.work-industry`, `.work-desc`, `.work-tags`, `.work-case-btn`), `keyland-compliance-group.html` (`.sys-grid`, `.sys-spec`, `.sys-shots`), `CLAUDE.md` (§4, §15), `LOGS.md`.

## 2026-08-06 — Bigger type + wider layout on the main portfolio
- **Report:** viewing `bryanodina.com` on a 13" MacBook Air, Bryan found text too small to read comfortably at ~0.5m and the hero wasting horizontal space (screenshot with arrows pointing at the left/right margins). Measured his screenshot against the CSS before proposing anything: his viewport is ~1700 CSS px, `--maxw` was `1180px`, so the container was leaving ~260px of dead margin per side — both complaints traced to the same root cause on the width side.
- **A third problem the audit found that he hadn't reported:** `.booking-widget` and `.form-card` both carried `zoom:0.7`, silently shrinking everything nested inside by 30%. The contact form's inputs were rendering at an effective ~10.2px and its labels at ~7.7px — likely the least readable text on the whole page.
- **Also found before proposing a fix:** all 61 `font-size` declarations in the file were hardcoded `px`, with zero `rem`/`em` anywhere and no `font-size` set on `html` or `body`. There was no single knob to scale type with — any fix needed either a full px→rem conversion or 61 individual edits.
- **Decisions confirmed with Bryan (asked rather than guessed, since each had real tradeoffs):** type scale +20% (root `120%`, i.e. 19.2px); container width 1560px; remove the `zoom:0.7` entirely rather than reduce it; apply to `index.html` only, leaving the Keyland page's sizing untouched.
- **Type scale, mechanically not by hand:** wrote a script that walked every `font-size:` declaration, recorded `(line, before, after)` for each, and converted px→rem at a 16px base — including the `px` terms inside `clamp()` (the `vw` middle term is left alone). Converted the 8 inline `style="font-size:…"` attributes and the 2 occurrences inside JS template strings too, so nothing on the page was left off the new scale. **Two deliberate exceptions kept as `px`:** `.hf-node-label`/`.hf-edge-label`, the hero flow-diagram's SVG `<text>` labels — these are user-space units tied to the SVG's own `viewBox`, not the document's root font size, and converting them would have desynced the labels from the diagram. Added `html{font-size:120%}` — `%` not a px value, so it composes with a visitor's own browser font-size preference instead of overriding it.
- **Width:** `--maxw` 1180px → 1560px. `.wrap`/`.nav` padding changed from a flat `24px` to `clamp(24px,2.5vw,40px)` — more breathing room on wide screens, still a real minimum gutter on narrow ones. `.hero p.lede` max-width 540→600px and `.section-head` max-width 660→720px, to keep line length in a comfortable range now that the columns are wider.
- **The one edit that had to be paired correctly:** the hero's "compact mode" breakpoint at `@media(max-width:1180px)` is a hardcoded duplicate of `--maxw`, meant to fire exactly when the viewport stops being wider than the container (the 2026-07-19 fix that keeps the CTA above the fold on 13" screens). Raised it to `max-width:1560px` in the same change — leaving it at 1180 while the container grew to 1560 would have reopened that exact bug for every width in between, which is a subtler regression than it sounds and easy to miss without knowing the history.
- **Removed `zoom:0.7`** from both `.booking-widget` and `.form-card`, keeping their `max-width` values. Combined with the type scale this takes the contact inputs to ~16.2px effective and labels to ~12.3px — both blocks have enough room in their containers at the new width to grow without overflowing.
- **Verification:** value-by-value check that every recorded conversion satisfies `rem × 16 == original px` (67/67 passed) — this is the check that actually matters, since a single fat-fingered `rem` value would be invisible to every other test. Grepped for any remaining `font-size:…px` and confirmed exactly 2 hits, both the intended SVG exceptions (by selector name, not line number — line numbers shifted after earlier edits in the file, which is exactly why the check re-derives them instead of trusting a hardcoded list). Confirmed `--maxw:1560px`, no stale `1180px` anywhere, the hero breakpoint synced, and `zoom:` absent from the whole file. HTMLParser tag-nesting clean, `node --check` clean, and re-ran the render harness to confirm `WORK_PROJECTS` still yields 22 byte-identical `.work-card` divs — this was meant to be a pure CSS/type change with zero markup or JS impact, and that's what the harness showed. Confirmed via `git status`/`git diff --stat` that only `index.html` changed, matching the agreed scope.
- **Still needed from Bryan:** check on the actual MacBook that the hero CTA buttons stay above the fold — that's the specific failure mode the raised breakpoint guards against, and it depends on his real window height, which can't be verified without a browser (Playwright isn't installed; adding it would put `node_modules` into a deliberately zero-dependency repo).
- Files touched: `index.html` only (`:root`, `html`, `.wrap`, `.nav`, hero rules, `.section-head`, `.booking-widget`, `.form-card`, and all 67 `font-size` declarations across the file). `CLAUDE.md` (§3 new subsection, §4), `LOGS.md`. `keyland-compliance-group.html` deliberately untouched.

## 2026-08-06 — Added "Vercel" Work filter category
- Bryan asked for a new Work-section filter tagging the four projects he confirmed run on Vercel: **Vanguard Credits, Wisdom Church of Manila, Celeste Nicolas Ministries, Affiliate Signup Alerts**. Appended `{ slug:'vercel', label:'Vercel' }` to `WORK_CATEGORIES` (now 12 entries incl. `all`) and added `'vercel'` to those four projects' `categories` arrays. No HTML/CSS/JS logic changes — the Work section has been data-driven since 2026-07-27, so `renderWork()` generates the button and the filter handler matches the slug generically.
- **Differs from how `n8n`/`claude` were added:** those tagged only the one new card they shipped alongside. This one applies to four pre-existing cards, because it's a hosting-platform grouping rather than a project category — the "don't retroactively cross-tag" convention doesn't apply here. Confirmed the intended card list with Bryan rather than inferring it from the copy.
- **Public-facing copy correction, sourced from Bryan directly:** Vanguard Credits' modal `description` said "A Flask-based credit repair platform deployed on **Railway**", which would have contradicted the new Vercel tag for anyone who filtered by Vercel and then opened that case study. Flagged the conflict before making any edit; Bryan confirmed Vercel is correct, so the wording was changed. The Railway claim was agent-drafted copy (see `CLAUDE.md` §8 item 6), not something he had ever reviewed — worth noting as a reminder that the unreviewed-copy backlog contains factual claims, not just style.
- **Scope decision (asked, not assumed):** "Vercel" is a **filter button only**. No `'Vercel'` pill was added to any card's visible `tags` row — Affiliate Signup Alerts already shows a `'Vercel serverless'` pill and the other three would have gained a stack-flavoured pill inconsistent with their capability-flavoured tags.
- **No `thumbAlt.vercel` overrides** on any of the four, so filtering by Vercel shows each card's default thumbnail. Verified this hits the existing graceful-fallback path (`alt[filter] || defaultSrc`) rather than breaking — same situation as `socialmedia`.
- **Verification:** extracted the inline `<script>` and ran it through `new Function()` (a stray comma in `WORK_CATEGORIES` would blank the entire Work section silently — this is the check that catches it). Then evaluated the data arrays directly and simulated the filter handler's token match to assert counts per filter: `vercel` → exactly 4, `all` → 22 unchanged, and every other category's count unchanged from before. Confirmed the four cards still appear under their pre-existing filters (`ai`/`apps`/`automation`/`claude`/`payments`) rather than being moved, checked the `data-thumbs` lookup falls back to default for all four, and asserted the string "Railway" no longer appears anywhere in the Vanguard object.
- Files touched: `index.html` (`WORK_CATEGORIES`, four `categories` arrays, one `description` string), `CLAUDE.md` (§4 item 8, §5 filter-categories paragraph + four table rows), `LOGS.md`.

## 2026-08-06 — New third page: `/connect` networking / QR landing page
- Bryan is handing out a **printed QR code** at the GoHighLevel anniversary event in Manila (8-9 Aug 2026) and needed a phone-first landing page: his photo, a short intro, and five social buttons. Built as `connect.html`, the site's third page, following the `keyland-compliance-group.html` standalone-page pattern (§15).
- **Decisions confirmed with Bryan before building** (all four had real tradeoffs): URL `/connect` — short, evergreen, typable if the QR fails; **stacked full-width button rows** over an icon grid or circle row; **evergreen copy with no event mention**, so the printed QR never goes stale and is reusable at future events; plus a small "View full portfolio" text link under the buttons. The URL choice was flagged as effectively irreversible before he picked it, since it gets printed on physical material.
- **Checked both images before designing around them** rather than assuming: `curl -I` for HTTP 200 + content-type, then parsed the PNG IHDR and JPEG SOF headers for true dimensions. This surfaced the design's actual risk — **the Level Up Academy logo is 372×220 landscape, a wordmark, not a square glyph.** Viewing it confirmed it reads "LEVEL UP ACADEMY" inside a winged shield. An unconstrained landscape raster in an intrinsic-height row would have stretched its own button taller than the other four, breaking the one thing Bryan asked for three separate times.
- **Same-size guarantee is held by three interlocking rules,** all asserted by a verification script: (1) `.link-btn` uses a **fixed `height`**, never `min-height`; (2) `.l-icon` is a fixed 44×30 box with `object-fit:contain` on the image — 44px wide is sized to the logo's real 1.69 aspect ratio, with the four SVG glyphs rendering 26×26 centred in the same slot; (3) `.l-label` is `nowrap` + ellipsis, since a wrapping label would also change row height. Accepted limitation: the wordmark's text isn't legible at icon scale — the row's text label carries the meaning, the silhouette carries the recognition.
- **Wrote the first icons in the repo.** There is no icon font or library anywhere, and before this the only `<svg>` in the codebase was the hero flow diagram — so the Facebook/TikTok/LinkedIn/YouTube glyphs are inline SVG written for this page, in brand colors so they read at a glance while the button chrome stays uniform.
- **Photo framing computed, not eyeballed:** the About-section photo is a 1014×1280 portrait whose face sits ~38% down, so a default centre crop would have framed his chest and the laptop. Worked out that `object-fit:cover` on a square box puts the crop centre at `p×0.208 + 0.396`, and set `object-position:center 10%` to land near the face with headroom.
- **The fold budget caught a real problem.** A script summing every declared height/margin against a 375px-wide phone showed the first copy draft wrapped to **three lines** for both the bio and roles, putting the page at ~743px default / ~629px compact — past the fold on both branches. Shortened the copy and tightened the compact branch to land at **~678px / ~536px**, against ~730px usable on an iPhone 12/13/14 and ~553px on an iPhone SE. `--btn-h` was held at **44px minimum** (Apple HIG tap target) and the budget tightened elsewhere instead. Note this is a *calculated* budget, not a measured one — no browser is available (Playwright isn't installed; adding it would put `node_modules` into a deliberately zero-dependency repo).
- **`min-height:100svh`, not `vh` or a fixed height** — iOS Safari's `vh` excludes the address bar and would push the card's bottom underneath it; `min-height` means overflow scrolls normally so nothing is ever cut off, the budget just aims to avoid scrolling. `@supports` fallback to `vh` for pre-2022 browsers.
- **Deliberate token divergence, documented so it doesn't get "fixed":** the `:root` block matches `index.html` on all 14 shared tokens (verified by diff), but `html{font-size:120%}` is **not** copied — that's the main portfolio's global type knob and would scale everything ~20%, pushing the buttons below the fold. A comment in the page's `<style>` and a paragraph in `CLAUDE.md` §16 both say so.
- **Routing:** added `{"source":"/connect","destination":"/connect.html"}` to `vercel.json`'s existing `rewrites` array. `functions` untouched, and **`cleanUrls` left off** per §15 — enabling it would make the working `/index.html` start 308-redirecting.
- **Not linked from the main nav**, deliberately — it's reached by QR scan and a nav entry would duplicate the footer socials. Two-line change to add later if Bryan wants it.
- **Verification:** 32-check structural suite — vercel.json parses with both rewrites and `functions` intact; exactly 5 buttons with all five hrefs **string-compared against the list Bryan supplied** (a typo here is invisible on screen and only surfaces when a contact taps it); `height` declared on `.link-btn` exactly once with no `min-height`/`max-height` and no per-button overrides; `:root` token parity vs `index.html`; leading-slash favicons; canonical; no event wording; tag nesting balanced. Plus the separate budget script above. All passed. **No browser check** — see the note on Playwright above.
- **Still needed from Bryan:** scan the real printed QR on his own phone before printing at volume — the fold budget is calculated, and his actual device height and browser chrome are the only real test. Also worth eyeballing the circular photo crop.
- Files touched: `connect.html` (new), `vercel.json` (one rewrite), `CLAUDE.md` (§1 now says three pages, new §16), `LOGS.md`.

## 2026-08-06 — Two new "Claude Projects": Clawd and LevelUp Academy Dashboard
- Added two new work cards under Claude Projects, both built with Claude Code: **Clawd**, an always-on-top Windows desktop widget (Electron, zero runtime dependencies) that shows live Claude usage — account rate limits, session token burn, context fill, cost, with a pixel-art mascot doubling as an ambient status light — and **LevelUp Academy Dashboard**, a private single-admin Next.js 15 / Supabase app tracking two revenue lines through Kanban boards into one combined P&L. Also tagged `vercel` and `apps` (Web Apps) on the dashboard, per Bryan's explicit instruction.
- **PII check on the LUA screenshots stopped the session before any data was wired in.** The 5 supplied screenshots show ~22 named individuals across Subaccounts/Students Kanban boards with payment status, overdue amounts and outstanding balances, plus a full business P&L (revenue, expenses, net loss, margin) — potentially the most sensitive set on the site if real. Flagged to Bryan before proceeding rather than assumed either way; **he confirmed all of it is seeded demo data**, not real clients or real numbers, so all 5 publish unblurred with no redaction needed. Recorded here explicitly so a future session doesn't re-flag this as a missed check.
- **Slug collision avoided deliberately.** "Level Up Academy" (slug `lua`) already exists as Bryan's own gamified course community. The new dashboard is a different product — his private tool for tracking that business financially — so it was given a distinct slug, `luadashboard`. `CASE_STUDIES` is built via `Object.fromEntries(WORK_PROJECTS.map(p => [p.slug, p]))`, so a duplicate slug would have silently made one card's modal unreachable while still rendering two cards; checked the full slug list for uniqueness after the edit.
- **Clawd's thumbnail is a self-hosted crop, not a CDN image — the first local asset added to `index.html` itself** (`images/vanguard/` previously existed only for `keyland-compliance-group.html`). The supplied 1909×1020 screenshot is mostly a VS Code window with the widget as a small translucent card in the lower-right corner; at work-card thumbnail size it would read as "a code editor," not "a desktop widget." Wrote a minimal PNG decode/crop/encode toolkit on Node's stdlib `zlib` only (same approach as the 2026-08-01 Vanguard redaction tooling, keeping the repo dependency-free): inflate IDAT, undo per-scanline filters, crop, re-filter, re-deflate, re-emit with correct CRCs. **Located the crop rectangle by pixel-probing the card's actual background color boundaries** (it's a translucent panel, so editor text shows *through* it — an early candidate crop still had text bleeding in from underneath, not beside, which cropping alone couldn't fix; correctly aiming at the card face itself resolved it), then iterated three candidate crops and viewed each before picking the final 456×285 rectangle — exactly `.work-thumb`'s `16/10` `aspect-ratio`, so the CSS crops nothing further. **Verified byte-for-byte against the source**: re-decoded the file as written to disk and diffed all 456×285×4 bytes against the original screenshot's pixels at the same offset — zero differences, confirming an exact crop rather than a re-render. Output: `images/clawd/clawd-widget.png`.
- **`renderWork()` previously hardcoded `CDN + p.thumb` at the `<img src>` and inside the `data-thumbs` map** (`index.html`), which only ever worked for bare CDN hashes. Added `thumbUrl(t)` — passes a `thumb` starting with `/` through unchanged, CDN-prefixes everything else — and a `loc(path, blur)` helper as the local sibling of the existing `im(hash, blur)`. **Verified as a real regression risk, not just in theory**: evaluated the live resolver against all 22 pre-existing projects and their 15 `thumbAlt` overrides and asserted every one resolves to the exact same URL as the old hardcoded expression, before trusting the new codepath for Clawd.
- **Copy** was drafted from Bryan's own written technical descriptions, the most detailed source material any case study on this site has had — architecture, defensive engineering, and the specific numbers he gave (21 assertions, ~1000× undercount risk, ₱8,500 course price, 7-day renewal flag, `@base-ui/react` vs Radix). **Two lines in his LUA writeup were session notes, not portfolio material** (the dev server having stopped, a commit still local and unpushed) — deliberately excluded from all copy fields; checked for afterward with a keyword scan.
- **Verification:** ran the inline `<script>` through `new Function()` (a missing comma on the previously-last `sssgrp` entry would have blanked the whole Work section); derived real counts from the evaluated data rather than assuming them — `WORK_PROJECTS` 22→**24**, `CASE_STUDIES` 32→**34**, `claude` 2→**4**, `vercel` 4→**5**, `apps` 5→**6**, `link:null` 7→**9** — and used those to correct every hardcoded count in `CLAUDE.md` §4/§5. Confirmed no `blur:true` on any of the 7 new images, confirmed both new images' CDN URLs plus the local crop path all return 200, and confirmed the resolver regression above.
- Files touched: `index.html` (`thumbUrl()`/`loc()` helpers, two `WORK_PROJECTS` entries, resolver call sites), `images/clawd/clawd-widget.png` (new), `CLAUDE.md` (§4, §5, §8), `LOGS.md`.

## 2026-08-06 — Digital business card (vCard), WhatsApp, and Book a call on /connect
- Bryan asked for a downloadable digital business card on `/connect`, framed explicitly around one goal: saving the card should put him in the other person's phone contacts, and because WhatsApp reads the phone's address book, he then appears there automatically — the priority was landing in their WhatsApp, not just "having a business card." Added a vCard download, a direct WhatsApp chat link, and — added mid-session at Bryan's request — a "Book a call" button.
- **The request grew from 6 buttons to 8 across the conversation, and 8 stacked full-width rows do not fit a phone screen.** Calculated the real numbers before proposing anything: 8 rows need ~780px against ~730px usable on an iPhone 12-class phone and ~553px on an SE — it fails on *every* phone, not just small ones. Presented Bryan the tradeoff (restructure vs. accept scrolling vs. drop something) rather than silently picking one; **he chose to restructure**: the three real calls-to-action (Save my contact / WhatsApp / Book a call) stay full-width and prominent, and the five social links collapse into one row of circular icon buttons. This supersedes the original "five identical buttons" contract from the page's first build — the same-size guarantee still holds, just within each group now instead of across all eight.
- **New budget is real, not marginal**: ~650px default / ~529px compact, against the same ~730px / ~553px targets — **80px and 24px of headroom**, versus the original 5-button layout's ~5px compact margin. Verified with the same budget-calculation script pattern used when this page was first built (deriving actual token/line values from the CSS rather than assuming them), extended to the new `.actions`/`.social-row` structure.
- **`bryan-odina.vcf` is vCard 3.0** (not 4.0 — 3.0 imports reliably across iOS Contacts, Android, and Google Contacts), generated by a new committed script, `scripts/build-vcard.js`, rather than hand-written, so it can be regenerated when details change. Contains name, `ORG`/`TITLE`, the WhatsApp number as `TEL`, email, site URL, `X-SOCIALPROFILE` entries for all 5 socials, and the existing About-section photo embedded as base64 `PHOTO` (fetched fresh from the CDN at generation time, encoded from raw bytes — no image-decoding dependency added).
- **RFC 6350 line folding was the highest-risk part of the vCard, so it got the most testing.** Every physical line must be ≤75 octets with continuation lines starting with a single space; the photo alone is ~170KB on one logical line, and some parsers silently reject an unfolded card. The fold function operates on octet boundaries and backs off from splitting any UTF-8 multi-byte sequence (`TITLE` contains "·"). Verified: CRLF line endings throughout, every physical line ≤75 octets (2289 lines checked), `BEGIN`/`VERSION`/`END` present, and — the check that actually matters — **the PHOTO field round-trips**: unfolded, base64-decoded, and compared byte-for-byte against the source JPEG fetched fresh from the CDN. Zero differences. Also cross-checked the `TEL`/WhatsApp values against the number already public in `index.html`'s footer, so the card can't silently drift from the site.
- **The download link deliberately carries no `download` attribute.** That attribute forces every browser — including iOS Safari — to save the file rather than open it, which suppresses Safari's native "Add Contact" preview sheet, the exact behavior this feature exists to trigger. Instead added a `headers` entry to `vercel.json` serving `/bryan-odina.vcf` as `Content-Type: text/vcard; charset=utf-8` with `Content-Disposition: inline`. Vercel doesn't map `.vcf` to a content type on its own, so this is the piece most likely to silently not work in production despite looking correct locally — flagged for explicit live verification (see below), not just a local check.
- **Two disclosure calls made explicitly, not by default.** The WhatsApp number is already public in both site footers (`index.html`, `keyland-compliance-group.html`), so putting it in the card and adding a direct `wa.me` chat button is not new exposure. The email (`bryanodina.ghl@gmail.com`) is genuinely new — no email has appeared anywhere on the site before this. Flagged to Bryan as a new disclosure before adding it; he chose to include it.
- **Verification:** re-ran the page's structural checks against the new markup (three action buttons with height declared exactly once on `.action-btn` and never overridden by `--primary`; five social buttons on a shared sized token; the LUA raster still capped by `object-fit:contain`; both new tap-target sizes ≥44px in the compact branch; all 5 social hrefs unchanged from before; the vCard href correct and `download`-free; tag nesting balanced; shared `:root` tokens still match `index.html`). Confirmed `vercel.json` still parses with both prior rewrites and `functions` intact, and `cleanUrls` still absent.
- **Still needed from Bryan:** scan the QR and tap "Save my contact" on his own phone before relying on it at an event — whether iOS actually opens the native Contacts sheet (vs. just downloading a file) is genuinely device/OS-version-dependent and can't be verified from here. Also worth a live `curl -I` check on the deployed `.vcf` (done as part of this session's Deploy Rules flow — see the shipping steps for the actual confirmation).
- Files touched: `bryan-odina.vcf` (new, generated), `scripts/build-vcard.js` (new), `.gitattributes` (new — see the autocrlf note above), `connect.html` (restructured `.links`/`.link-btn` → `.actions`/`.action-btn` + `.social-row`/`.social-btn`, new download/WhatsApp/calendar icons), `vercel.json` (new `headers` entry), `CLAUDE.md` (§16 rewritten for the new layout + new vCard/WhatsApp/booking subsection), `LOGS.md`.
- **Caught before shipping:** `core.autocrlf=true` on this Windows checkout silently LF-normalized `bryan-odina.vcf` on the first `git add` (175606 → 173317 bytes), which would have broken RFC 6350's CRLF requirement the moment Vercel's Linux build checked it out without reapplying the same conversion — a classic "works locally, wrong in production" bug that the vCard-parses checks couldn't catch since they ran against the pre-commit working-tree file. Added `.gitattributes` marking `*.vcf binary` so git never touches its line endings regardless of platform or `autocrlf` setting, then force-restaged the file and confirmed the git blob is now byte-for-byte identical to the verified working-tree copy.

## 2026-08-06 — Temporary "LIVE MANILA" event banner on /connect
- Bryan supplied a "LIVE MANILA" event graphic and asked for it above his photo on `/connect`. This directly contradicts that page's documented evergreen policy (`CLAUDE.md` §16), which exists specifically so the printed QR code stays valid after this one event. **Flagged the conflict explicitly before touching anything** — Bryan confirmed he wants it added anyway, and that he owns removing it himself once the event has passed. Treated as a time-boxed exception, not a policy change: the new markup/CSS/token are wrapped in `TEMPORARY` comments pointing back to this entry and to `CLAUDE.md` §16, so removal later is a clean one-block deletion rather than an archaeology exercise.
- **Checked the image before sizing anything around it.** Decoded the PNG's alpha channel directly (same practice as every other image added to this repo) and confirmed a fully transparent background (corner alpha = 0), so it sits cleanly on the page's dark background rather than showing as a white rectangle. Also noted it's hosted on a different GHL location ID (`bqd3A7Wt0iWPLpxnwLdJ`) than every other asset on the site (`7qfXIFSTdrRVqc8n8dWk`) — not a problem, just a new hotlink source worth knowing about if a future session wonders why one CDN URL doesn't match the pattern.
- **A literal `width:100%` banner does not fit this page's budget.** The image is 830×272 (3.05:1); at the card's real rendered width on a narrow phone that's ~110px tall, which alone exceeds the entire ~80px default headroom this page had before today. Sized it by `height:var(--banner-h)` + `width:auto` instead, so it reads as a wide banner without setting the page's vertical budget — same token-driven pattern as every other element on this page (`--avatar`, `--action-h`, etc.), with its own compact-branch override.
- **Iterated `--banner-h` against the page's budget-calculation script rather than picking a size by eye**, per this page's established practice. First pass (64px default / 40px compact) failed both branches — default landed 4px over the 10px-headroom bar, compact landed 24px past its budget entirely. Reduced to 54px default / 20px compact, and pulled `--avatar` down to 64px in the compact branch as a second lever once the banner alone wasn't enough there. Final: **~716px default / ~539px compact, both with exactly 14px headroom** against the ~730px/~553px targets.
- **Found and fixed a real bug in the budget script itself while doing this**, worth flagging since it affects trust in every prior budget number: the script had `.social-row`'s `margin-top` hardcoded as `18` for both branches, silently ignoring the compact media query's override to `12`. This happened to make past compact estimates *more conservative* than reality (undercounting available headroom, not overcounting it), so it didn't let a real overflow through — but it means the actual compact headroom on the vCard/WhatsApp/Book-a-call change earlier today was a few pixels better than what was reported at the time. Rewrote the script to derive every margin it uses (`.social-row`, `.portfolio-link`, `.avatar`, `.banner`) from the live CSS text instead of hardcoding any of them, and re-verified the fix by printing the extracted values and confirming they match what's actually in the file.
- **Verification:** re-ran this page's full structural suite (three action buttons unaffected, five social buttons unaffected, size contracts unchanged, tag nesting balanced) to confirm the banner addition didn't disturb anything already shipped — all still pass. Confirmed exactly one `<img class="banner">` in the actual body markup (a second regex hit turned out to be the removal-instructions comment mentioning the tag by name, not a duplicate element). Re-confirmed the image URL resolves with `HTTP 200`/`image/png`.
- Files touched: `connect.html` (`--banner-h` token + compact override, `.banner` rule, `<img class="banner">` markup, `.avatar` compact override tightened 72px→64px), `CLAUDE.md` (§16 — evergreen-exception subsection, updated budget figures, script-bug note), `LOGS.md`.

## 2026-08-07 — New "Tools I Use" logo section on the main portfolio
- Bryan asked for a section showing the tools he actually works with — a logo strip below Certifications, two rows, left-aligned on desktop and centred on mobile. Gave a dictated list (with a few names auto-corrected in transcription: "Versace"→Vercel, "Superbase"→Supabase, "Slap"→Slack, "Excalidor"→Excalidraw, "N eight n"→n8n) plus asked me to suggest more. "Send it" in his dictation matched no identifiable tool and was dropped rather than guessed at.
- **Sourced every logo before proposing anything**, not after: checked all 31 candidate slugs against Simple Icons (CC0-licensed, `cdn.simpleicons.org`) up front. 27 resolved cleanly as single-path SVGs with the official brand hex in `fill` — exactly what a monochrome-then-brand-color-on-hover treatment needs. **4 did not exist there** — Slack and OpenAI were removed from Simple Icons on trademark request, and GoHighLevel (Bryan's primary platform) was never listed. Flagged this to Bryan with the exact list before building anything, rather than silently substituting or omitting.
- **Decisions confirmed with Bryan:** monochrome default, real brand color on hover, logo-only tiles (no text labels), and — critically — that the 4 missing logos should be sourced and self-hosted by the agent (shown to him before shipping) rather than left as a manual task or dropped. Also approved the suggested additions: Next.js, TypeScript, Tailwind, Stripe, Xendit, Discord, Notion, Figma. He explicitly did not select GitHub/Flask/Electron or Canva, so those were left out despite being technically documented elsewhere in this repo.
- **Two real problems surfaced while sourcing the 4 missing logos, both put back to Bryan rather than resolved silently:**
  1. Slack's real mark is inherently 4-colored (no official single-fill version exists) — doesn't fit the monochrome/hover pattern used for the other 30. Bryan chose to keep it in full color always, as the one deliberate exception.
  2. GoHighLevel has no usable vector logo anywhere — checked their own site (including brand/press pages, which just redirect), GitHub, Wikimedia, Simple Icons, and Brandfetch. The only asset that exists on GHL's own domain is a 16×16 raw-bitmap favicon, unusable at any real size. Bryan supplied a real GHL integration partner's page (globalcallforwarding.com) that carried a proper 640×355 transparent-background "HighLevel" lockup.
- **GoHighLevel asset — cropped, not the full lockup.** Needed just the three-arrows glyph, not the "HighLevel" wordmark. An automated column-gap scan to find the glyph/wordmark boundary gave misleading results (confused by the arrows' own drop-shadow layers), so switched to the same iterate-crop-and-view approach used for the Clawd screenshot in the previous session: wrote several candidate crops at different right edges and viewed each until the boundary was unambiguous. Final crop (156×154, transparent) **verified byte-for-byte against the source region — 0 differences** — using the same hand-rolled zlib-based PNG decode/crop/encode tooling as the Vanguard redactions and the Clawd thumbnail. Self-hosted at `images/tools/gohighlevel.png`, the second local image referenced by `index.html` itself.
- **VS Code and ChatGPT/OpenAI marks extracted programmatically, not hand-traced.** VS Code's silhouette path was pulled from Microsoft's own SVG and cross-checked against a second independent source (a different logo repo) that encodes the identical shape — agreement between two unrelated sources was treated as confirmation neither is a stale copy. OpenAI's file was a combined wordmark+glyph lockup; isolated just the icon-mark path (not the 6 letterform paths) and computed its own tight bounding box by walking the path's move/line/curve commands, rather than reusing the wordmark's much wider viewBox.
- **No way to preview an SVG directly in this environment, so a real headless-Chrome render pipeline was built to verify every logo before use** — same "view it, don't guess" standard applied throughout this repo's image work. Found and used the machine's existing Chrome install (no new dependency installed). This caught a real mistake before it shipped: an early GoHighLevel crop candidate visually rendered a stray black letterform that automated column-scanning had missed; re-cropping and re-viewing fixed it. All 31 final logos were rendered together in one sheet and visually confirmed correct/recognizable before wiring anything into `index.html`.
- **Dark-background color problem, caught and fixed before shipping, not after:** Vercel, Next.js and Notion's Simple Icons brand hex is `#000000`; ChatGPT's mark has no single official hue and is commonly rendered solid black. All four would have gone *invisible* on hover against this page's near-black background — the opposite of the intended highlight effect. Overrode just those four to the site's `--text` white, which is each brand's own standard presentation on dark surfaces (not a fabricated substitute), and did it in the data itself so the override is visible rather than hidden in a CSS exception.
- **`renderTools()` placed immediately after `renderWork()`'s closing**, before the `.reveal` IntersectionObserver setup — same ordering constraint documented for the Work section, since the observer snapshots `.reveal` elements once on load and would silently skip anything rendered after it runs. Verified the source-order position of `renderTools` is numerically before `IntersectionObserver` in the built script.
- **Layout tuned to an exact row count, not approximated:** `.tools-grid` uses fixed 76px tracks (not `1fr`) with `justify-content:start`, sized so 16 tiles fit per row at `--maxw`'s full content width — landing all 31 tools in exactly two rows (16 + 15) on a wide desktop, matching what Bryan asked for. `@media(max-width:879px)` switches to `justify-content:center`, reusing the site's existing nav breakpoint rather than inventing a new one, for the mobile-centered behavior he asked for. Documented as an honest limitation: narrower desktop/laptop widths will show more than two rows as the grid naturally reflows — correct responsive behavior for 31 items, not something a fixed layout could avoid.
- **Verification:** inline script parses via `new Function()`; evaluated `TOOLS` directly and asserted all 31 entries present, no duplicate names, every path/brand-hex/viewBox or raster/multi entry well-formed; confirmed the render-before-observer ordering by comparing source positions; confirmed the Slack color-exclusion CSS (`:not(.tool-svg--color)`) is wired correctly in both directions and applies to exactly one tile. Rendered the real CSS/JS from `index.html` (not a re-implementation) in an isolated harness via headless Chrome to visually confirm: two rows at desktop width, correct monochrome-default rendering for all 31 tiles including the GoHighLevel raster (a path-format mismatch in the harness itself — an absolute `/images/...` path resolving against the filesystem root under `file://`, not against the site root as it correctly does on Vercel — caused one false "broken image" during testing, resolved by confirming the real deployed path format separately), the hover-to-brand-color mechanism via `--brand` custom properties, and mobile centering via the 879px breakpoint.
- Files touched: `index.html` (new `#tools` section HTML, `.tools-grid`/`.tool` CSS, `TOOLS` data array + `renderTools()`), `images/tools/gohighlevel.png` (new), `CLAUDE.md` (§4 renumbered, new §6a), `LOGS.md`.

## 2026-08-07 — Fixed clipped ChatGPT logo, replaced Slack asset
- Bryan reported the ChatGPT tile in the new "Tools I Use" section was visibly cropped on the left side, and supplied a screenshot showing it. Separately asked for a specific replacement Slack asset (`cdn.creazilla.com/icons/3214678/slack-logo-icon-lg.png`).
- **Root-caused the ChatGPT clipping rather than nudging the padding.** The icon's viewBox was computed by a hand-rolled SVG path bounding-box walker written the previous session. Re-rendered the path with the computed bbox drawn as a visible outline and confirmed visually that the glyph extended past the outline on multiple sides — the walker was wrong, not just tight. Found two real bugs: (1) it never handled the `V`/`v` (vertical lineto) command, which this specific path uses, silently dropping those tokens and desyncing everything parsed afterward; (2) it didn't handle implicit repeated command arguments (a coordinate pair with no new command letter, valid SVG shorthand), which would misread a bare number as a command and skip it. **Fixed by not hand-parsing at all** — used the browser's own `SVGGraphicsElement.getBBox()` via headless Chrome, which is spec-correct by construction. Re-rendered the corrected viewBox at both a large preview size and the real 34×34 tile size and confirmed the glyph is now fully contained and centered on every side before touching `index.html`.
- **This is a standing lesson for this codebase, not just a one-off fix:** any future tile needing a computed viewBox from raw path data should use `getBBox()` in a real (or headless) browser, not a hand-rolled path-command walker — recorded in `CLAUDE.md` §6a so a future session doesn't repeat the same mistake on a different logo.
- **Slack swapped to the asset Bryan supplied.** Fetched and inspected it before using it: 1280×1280, 4-bit indexed-color PNG. My existing hand-rolled PNG decoder (built for 8-bit depth, used for the GoHighLevel/Clawd crops) doesn't support 4-bit indexed PNGs, so used the browser's canvas API instead (`drawImage` + `getImageData`) to read pixel data — confirmed the background is genuinely transparent (alpha 0 at all corners sampled; my own image-preview tool composites transparency onto white by default, which had made it look opaque at first glance). Cropped to the icon's tight opaque bounds via canvas (1054×1054 with 8px pad), **verified pixel-for-pixel against the source region — 0 differences** — then downscaled to 200×200 via high-quality canvas resampling for a reasonable file size (10.5KB, comparable to the GoHighLevel asset). Viewed the result at both full crop size and the downscaled size before wiring it in.
- **This let Slack drop out of being a permanent special case.** The previous Slack entry was the official 4-color mark, kept in full color always because no single-fill official version exists — the one tile that didn't participate in the row's muted-then-brand-color-on-hover pattern. The new two-tone asset fits that pattern like every other raster tile (GoHighLevel), so it now uses the same `filter:grayscale(1) opacity(.65)` → `filter:none` treatment. Removed the now-dead `multi` data shape and `.tool-svg--color` CSS exclusion rules from both the JS render function and the CSS — they had exactly one consumer (the old Slack entry) and nothing else will ever use them, so left-behind dead code was removed rather than kept "just in case."
- **Verification:** re-ran the full data-integrity suite (31 entries, no duplicates, every path/brand/viewBox or raster entry well-formed); confirmed zero remaining references to `multi`/`tool-svg--color` anywhere in the file; re-rendered the real CSS/JS from `index.html` in the same isolated harness used in the previous session and visually confirmed both fixes — ChatGPT fully uncropped, Slack showing the new muted grey mark matching the row.
- Files touched: `index.html` (ChatGPT `vb` corrected, Slack entry changed from `multi` to `img`, dead `multi`/`.tool-svg--color` code removed from both CSS and JS), `images/tools/slack.png` (new), `CLAUDE.md` (§6a rewritten for the current state), `LOGS.md`.

## 2026-08-07 — Removed Slack from Tools I Use
- Bryan called the Slack tile "an eyesore" and asked for it removed, no replacement requested. This is the second Slack asset shipped and removed the same day — the original 4-color official mark was replaced with a two-tone raster crop earlier in the session, and now that's gone too.
- Removed the `TOOLS` entry, deleted `images/tools/slack.png` (the now-orphaned asset from the earlier swap), and updated every count reference from 31→30 tools across `index.html`'s comments and `CLAUDE.md` §6a. GoHighLevel is now the only raster tile in the set (was 2 of 31, now 1 of 30).
- **Recorded explicitly in `CLAUDE.md` so a future session doesn't re-add Slack using either prior asset without asking first** — both were deliberately rejected, not superseded by a technical problem. The `multi`/`.tool-svg--color` code path from the very first Slack version had already been deleted when it was replaced; nothing Slack-related remains anywhere in the codebase now.
- **Verification:** inline script still parses; confirmed zero remaining references to `'Slack'` or `slack.png` anywhere in `index.html`; re-evaluated `TOOLS` directly and confirmed exactly 30 entries, no duplicates, and GoHighLevel as the sole raster tile. 16-per-row layout math is unaffected (16 + 14 = 30, still two rows), so no CSS retuning was needed.
- Files touched: `index.html` (removed the `TOOLS` entry + updated comments), `images/tools/slack.png` (deleted), `CLAUDE.md` (§4, §6a), `LOGS.md`.

## 2026-08-07 — New "Desktop Apps" category + GHL Inbound Webhook Tester project
- Bryan asked for a new Work card ("GHL Inbound Webhook Tester" — a desktop app he built to test GoHighLevel inbound-webhook triggers before wiring a real landing-page form to one, since no easy tool for that existed) and a new filter category, "Desktop Apps", tagging both the new card and the existing Clawd card (previously `claude`-only). Data-only change: one appended `WORK_CATEGORIES` entry (`desktop`), one appended `WORK_PROJECTS` object (`ghlwebhook`), and one category added to Clawd's existing array.
- **PII/security check stopped the session before wiring the image in, per the standing rule in `CLAUDE.md` §9.** The single supplied screenshot shows: a new email, `bryan@levelupacademy.cc` — not previously disclosed anywhere on the site (only `bryanodina.ghl@gmail.com`, on `/connect`, was public before) — and what reads as a live GHL webhook-trigger URL, a real endpoint that could theoretically be POSTed to by anyone who saw the image. Flagged both explicitly rather than assuming either was fine. **Bryan confirmed both**: the email can stay visible, and the webhook URL is just a test-trigger endpoint, fine to publish as-is. The visible name/phone in the screenshot are his own test data — the phone number matches the WhatsApp number already public in the site footer and vCard, so it isn't a new disclosure.
- **Counts derived from the live data, not assumed, before writing any doc update:** `WORK_PROJECTS` 24→**25**, `WORK_CATEGORIES` 12→**13**, `link:null` count 9→**10**, and the `desktop` filter confirmed to match exactly 2 cards (Clawd, GHL Inbound Webhook Tester) — used these real numbers to correct every hardcoded count in `CLAUDE.md` §4/§5/§9 rather than incrementing by hand.
- **Verification:** ran the inline `<script>` through `new Function()` (the previously-last entry needed a new trailing comma — the classic silent-failure point for this kind of append); re-evaluated the data and asserted slug uniqueness, the desktop-filter match count, no `blur:true` on the new image, and `link:null` on the new card; re-confirmed the CDN image resolves (200, `image/png`).
- Files touched: `index.html` (`WORK_CATEGORIES`, Clawd's `categories`, new `ghlwebhook` entry), `CLAUDE.md` (§4, §5), `LOGS.md`.
